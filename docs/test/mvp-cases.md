# MVP 验收用例

对应需求：`docs/requirements/20260320-局域快传-mvp.md`  
当前交付物是 Qt 客户端，不是 Go 里那套网页接口。下面标的是 2026-09-21 在本机对 Qt 包的执行结果。

### TC-01 文本收发与中文
- 对应验收：A3
- 前置：Windows 包 `dist/windows-x64/landrop.exe` 已启动
- 步骤：向 `POST /api/inbox` 投递 UTF-8 中文
- 期望：接口返回成功，对端聊天区能看到 UTF-8 中文
- 类型：集成
- 自动化：`powershell -File tests\api-smoke.ps1`
- 结果：通过。2026-09-21 Windows（omen / 10.100.20.223）向麒麟（qwer-pc / 10.100.20.22）投递「双机验收 dual-110527」，麒麟聊天区可见。反向「麒麟回传 110548」出现在 Windows 聊天区

### TC-02 文件落盘、中文名、不逃出目录、同名不覆盖
- 对应验收：A4 的落盘部分
- 前置：临时工作目录
- 步骤：上传 `说明.txt`；再以 `../../outside.txt` 上传；再传一次同名
- 期望：文件在下载目录内，中文名保留，穿越被收成基本名，同名不覆盖
- 类型：集成
- 自动化：`tests\api-smoke.ps1`；另有 `--self-check` 覆盖文件名裁剪和网卡过滤
- 结果：通过

### TC-03 设置能被读到
- 对应验收：A5
- 前置：已有 `%AppData%\lan-drop\settings.json`，设备名 omen
- 步骤：启动后看 `GET /api/info`
- 期望：返回的名称是配置里的设备名
- 类型：集成
- 结果：通过（返回 `omen`）。改设置后再读回、以及界面保存，本轮没做

### TC-04 自检：路径、虚拟网卡、广播地址
- 对应验收：A1 的协议辅助，不是双机发现
- 步骤：`landrop.exe --self-check`；ARM 包在交叉编译脚本里用 qemu 跑同一自检
- 期望：退出码 0
- 类型：单元
- 结果：Windows 通过。ARM 包在打包时自检通过。注意：无控制台的 exe 不能用 PowerShell 的 `$LASTEXITCODE` 判断，必须看进程退出码

### TC-05 手动加节点与探测
- 对应验收：A2；P1–P3（`docs/requirements/20260922-manual-peers-persist.md`）
- 步骤：点「+ 加 IP」添加可达节点 → 看列表 → 完全退出再启动
- 期望：列表出现该节点；`settings.json` 有 `manualPeers`；重启后节点仍在（别名/系统类型保留）
- 类型：手测 / 集成
- 结果：自检覆盖读写往返；界面重启待手测

### TC-37 删除手动节点
- 对应验收：R1–R4（`docs/requirements/20260922-remove-manual-peer.md`）
- 步骤：添加手动节点 → 右键「删除手动节点」确认 → 再对自动发现节点右键/按 Delete → 重启
- 期望：手动节点消失且 settings 无该项、重启不恢复；自动发现节点不可删
- 类型：手测 / 集成
- 结果：自检覆盖 removeManual；界面待手测

### TC-06 同网段自动发现
- 对应验收：A1
- 前置：两台机器，放行 TCP 8848 与 UDP 8850
- 步骤：两边运行，等约 60 秒
- 期望：设备列表出现对端
- 类型：手测
- 结果：通过。2026-09-21 两边列表都是 1 台在线：Windows 看到 qwer-pc 10.100.20.22:8848，麒麟看到 omen 10.100.20.223:8848。文字与小文件双向都落到了对端聊天区和下载目录（Windows `downloads/from-kylin-now.txt`，麒麟 `~/landrop/downloads/from-windows.txt`）

### TC-07 大文件
- 对应验收：A4
- 步骤：本机向自己的 `POST /api/upload` 送 100MB
- 期望：落下的文件长度为 100MB；进程工作集不按文件大小上涨
- 类型：集成（只覆盖接收落盘，不覆盖窗口里点「发送文件」，也不是两台机器）
- 结果：通过。文件 100MB，工作集大约增加 0.4MB

### TC-08 网页共享列表与下载
- 对应验收：A9
- 前置：用环境变量 `LANDROP_SHARE_DIR` 指向含 `hello-share.txt` 的目录启动
- 步骤：`GET /share/`；再下载 `/share/hello-share.txt`
- 期望：列表含该文件名；下载内容正确
- 类型：集成
- 自动化：`tests\api-smoke.ps1`
- 结果：通过（`tests\api-smoke.ps1`，2026-09-21）

### TC-09 设备类型映射
- 对应验收：U2；O1–O3（`docs/requirements/20260922-arm-os-not-rpi.md`）；A1–A3（`docs/requirements/20260922-avatar-name-initial.md`）
- 步骤：`landrop.exe --self-check`；看列表与聊天头像
- 期望：windows→笔记本角标、android→手机、iPadOS→平板；头像为机器名首字（如 qwer-pc→Q），非系统 SVG「树」；发出气泡头像为本机设备名首字
- 类型：单元 + 手测
- 结果：随 self-check；头像待手测

### TC-10 顶栏广播 Logo 左右弧
- 对应验收：U1
- 类型：手测
- 步骤：启动后看左上角蓝底图标，弧应在圆点左右
- 结果：待目视确认

### TC-11 输入区 Enter / Shift+Enter
- 对应验收：U5
- 类型：手测
- 步骤：看输入区右上角提示；再在输入框按 Enter 应发送，Shift+Enter 换行
- 期望：`Enter`、`Shift+Enter` 是浅底深灰边框的键帽，不是普通灰字
- 结果：键帽改为独立标签后待目视确认

### TC-12 发送文件夹与抖动
- 对应验收：U6、U7；文件夹说明见 TC-62
- 类型：手测
- 步骤：点「发送文件夹」选含若干文件的目录；点「抖动窗口」看本机与对端是否轻抖
- 结果：待手测

### TC-62 发文件夹仅顶层说明
- 对应验收：D1–D4（`docs/requirements/20260922-folder-top-only-hint.md`）
- 步骤：① 仅顶层文件的目录 → 看系统条；② 顶层有文件+子目录 → 取消一次再确认；③ 仅子目录无顶层文件；④ 拖文件夹进聊天
- 期望：系统条含「顶层/不含子目录」；有子目录时先确认；仅子目录有说明不发；拖放多文件也标明仅顶层
- 类型：手测
- 结果：待手测

### TC-13 会话顶栏与直连横幅
- 对应验收：C1、C2、C3（`docs/requirements/20260921-chat-session-header.md`）
- 前置：启动 Windows 包，左侧「加 IP」添加任意可达或不可达地址（如 `127.0.0.1:8848`）并选中
- 步骤：观察右侧消息区上方
- 期望：出现对端头像、名称、地址胶囊、「即时聊天」「文件传输 (0)」按钮，以及绿色「已建立局域网直连：…」横幅
- 类型：手测
- 结果：待目视确认

### TC-14 即时聊天 / 文件传输页壳切换
- 对应验收：C4
- 前置：已选中对端
- 步骤：点「文件传输 (0)」再点「即时聊天」
- 期望：文件页显示占位说明；聊天页回到消息与输入区；当前按钮高亮切换
- 类型：手测
- 结果：待目视确认

### TC-15 切换对端时顶栏同步
- 对应验收：C5
- 前置：列表中至少两台对端（发现或手动添加）
- 步骤：依次点选不同对端
- 期望：顶栏名称、地址、头像与横幅文案随之变化
- 类型：手测
- 结果：待目视确认

### TC-16 加 IP 对话框布局
- 对应验收：D1、D2（`docs/requirements/20260921-add-peer-dialog.md`）
- 步骤：点左侧「+ 加 IP」
- 期望：标题为「跨网段直连 / 手动添加节点」，有副标题、蓝地球图标、IP|端口、别名、系统类型|部门标签、测试连通链接、取消与「+ 添加并连接」
- 类型：手测
- 结果：待目视确认

### TC-17 对话框探测与添加
- 对应验收：D3、D4、D5（`docs/requirements/20260921-add-peer-dialog.md`）
- 步骤：端口保持 8848；点测试连通填 `127.0.0.1`；再选系统类型添加；部门标签随便填
- 期望：探测有连通或失败文案；添加后列表出现节点且头像随所选系统类型
- 类型：手测
- 结果：待手测

### TC-58 手动节点部门标签入库
- 对应验收：T1–T5（`docs/requirements/20260922-peer-tag.md`）
- 步骤：加 IP 填标签「工控」→ 看 settings.json → 退出再开 → 搜索「工控」；另测不填标签；`--self-check`
- 期望：tag 落盘且重启可见；列表/顶栏有标签；搜索可筛；无 tag 旧配置不崩；自检通过
- 类型：手测 / 集成
- 结果：`--self-check` 含 tag；界面待手测

### TC-60 右键编辑手动节点
- 对应验收：E1–E5（`docs/requirements/20260922-edit-manual-peer.md`）
- 步骤：手动节点右键「编辑别名 / 标签…」改别名与标签 → 看列表/顶栏 → 退出再开；另对自动发现节点确认无编辑或禁用；`--self-check`
- 期望：预填当前值；保存后展示与 settings 同步；重启仍在；自动发现不可编；自检覆盖同址覆盖 tag
- 类型：手测 / 集成
- 结果：`--self-check` 含覆盖；界面待手测

### TC-18 加 IP 对话框圆角无黑三角
- 对应验收：D7
- 步骤：点「+ 加 IP」，看弹窗外圈四角
- 期望：圆角外透出主窗口内容，无黑色直角三角；关闭为清晰叉号图标
- 类型：手测
- 结果：待目视确认

### TC-19 系统类型下拉样式
- 对应验收：D8
- 步骤：打开加 IP 对话框，看「系统类型」并点开列表
- 期望：外观与别名输入框同系（浅底圆角细边 + 细线箭头）；弹出列表无系统原生凹槽条
- 类型：手测
- 结果：待目视确认

### TC-20 网页共享按钮地球图标
- 对应验收：U8（`docs/requirements/20260321-ui-icons.md`）
- 步骤：启动后看顶栏「网页共享 (HTTP)」
- 期望：按钮左侧为蓝色地球线性图标，不是空白或其它符号
- 类型：手测
- 结果：待目视确认

### TC-21 设置对话框布局
- 对应验收：S1、S2、S3、S6、S8（`docs/requirements/20260921-settings-dialog.md`）；B1（`docs/requirements/20260922-settings-browse-dir.md`）；H1（`docs/requirements/20260922-threads-honest.md`）；A1（`docs/requirements/20260922-copy-honest-search.md`）
- 步骤：点顶栏设置齿轮
- 期望：标题「局域快传设置」、副标题含「通知偏好」（非仅「接收提示」）、深色齿轮徽标；名称/端口|线程数（灰显+暂不生效说明）/下载目录+「浏览…」及说明；两项右对齐开关；仅「保存并关闭」；四角无黑三角
- 类型：手测
- 结果：待目视确认

### TC-65 搜索占位与无匹配空态
- 对应验收：A2–A4（`docs/requirements/20260922-copy-honest-search.md`）
- 步骤：看搜索占位；有对端时输入不可能匹配的关键字；再清空搜索
- 期望：占位含标签；无匹配时右侧有说明；清空后列表恢复
- 类型：手测
- 结果：待手测

### TC-66 上传中非打断提示
- 对应验收：B1–B4（`docs/requirements/20260922-busy-upload-soft.md`）
- 步骤：发大文件过程中再点发送文件/拖入文件/点旧失败重试；看是否弹框；传完后再发一次
- 期望：无模态框；当前传输继续；成功路径以入队系统条为主（见 TC-67）；传完可再发
- 类型：手测
- 结果：待手测

### TC-67 发送中自动入队
- 对应验收：Q1–Q5（`docs/requirements/20260922-upload-queue-append.md`）
- 步骤：发大文件过程中再拖入 2 个不同文件；再点发送选一个；再拖同一路径一次；另测一次发送中点失败重试；再测中途人为断网使当前失败
- 期望：系统条「已加入发送队列」；传完自动续传；重复路径不重复排队；重试无模态且入队；失败后剩余队列清空不再续传
- 类型：手测
- 结果：待手测

### TC-68 取消当前发送
- 对应验收：C1–C5（`docs/requirements/20260922-cancel-upload.md`）
- 步骤：发大文件并再入队 1～2 个；看进度旁「取消」；点取消；切到文件页再发一次看取消；取消后再发一个小文件
- 期望：发送中可见取消、空闲隐藏；点后停止且排队不续；系统条「已取消发送」无重试；文件页同样可取消；取消后可再发成功
- 类型：手测
- 结果：待手测

### TC-69 进度显示排队剩余
- 对应验收：R1–R3（`docs/requirements/20260922-upload-queue-remain.md`）
- 步骤：发大文件过程中再入队 2 个看进度文案；等传完一个再看剩余数；取消一次看进度是否清空
- 期望：入队后出现「排队还剩 N 个」；N 随完成递减；最后一项无排队后缀；取消后无残留
- 类型：手测
- 结果：待手测

### TC-70 发送文件多选
- 对应验收：M1–M4（`docs/requirements/20260922-send-file-multiselect.md`）
- 步骤：点发送文件一次选 3 个；发送中再点发送多选 2 个；再开对话框点取消
- 期望：对话框可多选；空闲多选依次发并有开始发送系统条；忙时入队续传；取消无动作
- 类型：手测
- 结果：待手测

### TC-71 粘贴剪贴板图片发送
- 对应验收：I1–I4（`docs/requirements/20260922-paste-clipboard-image.md`）
- 步骤：选对端后 Win+Shift+S 截图再 Ctrl+V；再复制一个文件 Ctrl+V；再复制文字 Ctrl+V；未选对端时粘贴截图
- 期望：截图发成 PNG；文件粘贴仍发文件；文字进输入框；未选对端有提示
- 类型：手测
- 结果：待手测

### TC-72 打开所在目录并选中
- 对应验收：S1–S3（`docs/requirements/20260922-reveal-select.md`）
- 步骤：收或发一个文件后点「打开所在目录」；再点「打开文件」；删文件后再点所在目录
- 期望：资源管理器高亮该文件；打开文件仍用默认程序；已删路径无崩溃
- 类型：手测
- 结果：待手测

### TC-73 选中对端聚焦输入框
- 对应验收：F1–F2（`docs/requirements/20260922-focus-input-on-peer.md`）
- 步骤：点列表某对端直接打字；再切到另一对端继续打字
- 期望：无需再点输入框即可输入
- 类型：手测
- 结果：待手测

### TC-74 搜索清空与 Ctrl+F
- 对应验收：E1–E3（`docs/requirements/20260922-search-clear-focus.md`）
- 步骤：搜索框输入关键字看 ×；点 ×；再输入后 Esc；在输入框按 Ctrl+F
- 期望：× 可清空；Esc 清空；Ctrl+F 聚焦搜索并选中文字
- 类型：手测
- 结果：待手测

### TC-75 离线发送轻提示
- 对应验收：O1–O3（`docs/requirements/20260922-offline-send-hint.md`）
- 步骤：选离线对端发文字；再发文件；再选在线对端发文字
- 期望：离线首次有系统条且不拦发送；同对端不重复；在线无此条
- 类型：手测
- 结果：待手测

### TC-76 文件卡复制路径
- 对应验收：P1–P3（`docs/requirements/20260922-copy-file-path.md`）
- 步骤：收/发各一个文件后点「复制路径」粘贴到记事本；看发送中进度卡
- 期望：剪贴板为绝对路径；进度中无该链接
- 类型：手测
- 结果：待手测

### TC-77 文字气泡复制
- 对应验收：T1–T3（`docs/requirements/20260922-text-bubble-copy.md`）
- 步骤：收/发普通文字点「复制」；再发 ``` 代码块用块内复制
- 期望：普通文字剪贴板正确；代码块行为不变
- 类型：手测
- 结果：待手测

### TC-78 失败后重发剩余队列
- 对应验收：Q1–Q4（`docs/requirements/20260922-retry-remaining-queue.md`）
- 步骤：对不可达对端一次入队 3 个文件等失败；看失败条；分别试「重试」与「重发剩余」
- 期望：有「重发剩余 3」；重试只一个；重发剩余三个入队；单文件失败无该链
- 类型：手测
- 结果：待手测

### TC-79 发送进度速率
- 对应验收：V1–V3（`docs/requirements/20260922-upload-speed.md`）
- 步骤：发较大文件看进度；再入队看排队文案；传完或取消
- 期望：出现 xx/s；可与排队并存；结束后进度清空
- 类型：手测
- 结果：待手测

### TC-80 关闭进托盘可配置
- 对应验收：C1–C3（`docs/requirements/20260922-close-to-tray-option.md`）
- 步骤：默认点 ✕ 看托盘；设置取消「关闭窗口时最小化到托盘」保存后再点 ✕；看 settings.json 的 closeToTray
- 期望：默认进托盘；取消后退出进程；配置持久
- 类型：手测
- 结果：待手测

### TC-81 复制成功提示
- 对应验收：F1–F3（`docs/requirements/20260922-copy-feedback.md`）
- 步骤：点文字「复制」、文件「复制路径」、代码块「复制」
- 期望：分别出现已复制 / 已复制路径提示，剪贴板正确
- 类型：手测
- 结果：待手测

### TC-82 接收进度与速率
- 对应验收：R1–R4（`docs/requirements/20260922-recv-speed.md`）
- 步骤：对端发较大文件看本机进度区；本机发送中再让对端发一个看是否仍显示发送
- 期望：空闲收文件时有「正在接收」与大致速率；收完清空；发送优先
- 类型：手测
- 结果：待手测

### TC-83 清空发送排队
- 对应验收：Q1–Q3（`docs/requirements/20260922-clear-upload-queue.md`）
- 步骤：发大文件再入队 2 个；点「清空排队」；等当前传完；再测「取消」
- 期望：清空后当前继续、排队不再发；按钮消失；取消仍可停当前
- 类型：手测
- 结果：待手测

### TC-84 传输剩余时间
- 对应验收：E1–E3（`docs/requirements/20260922-transfer-eta.md`）
- 步骤：传/收较大文件看进度；刚开始或极慢时再观察
- 期望：速率稳定出现「约 …」；过慢可不显示；收发均可
- 类型：手测
- 结果：待手测

### TC-85 设备列表排序
- 对应验收：S1–S3（`docs/requirements/20260922-peer-list-sort.md`）
- 步骤：同时有在线/离线与未读；刷新列表；保持当前会话
- 期望：在线在前，未读优先，选中不丢
- 类型：手测
- 结果：待手测

### TC-86 托盘通知声开关
- 对应验收：M1–M3（`docs/requirements/20260922-tray-mute.md`）
- 步骤：托盘勾/取消「通知声」；收消息听声；重启；再对设置页
- 期望：立刻静音/开声；持久；与设置同步
- 类型：手测
- 结果：待手测

### TC-87 窗口置顶
- 对应验收：A1–A3（`docs/requirements/20260922-always-on-top.md`）
- 步骤：设置开启「窗口置顶」保存；盖住其它窗口；关闭；重启
- 期望：置顶生效；关闭恢复；重启保持
- 类型：手测
- 结果：待手测

### TC-88 MainWindow 模块拆分冒烟
- 对应验收：M1–M4（`docs/requirements/20260922-split-mainwindow.md`）
- 步骤：Release 启动；收发文字与文件；点文件卡打开/复制路径；跑 `--self-check`
- 期望：行为与拆分前一致；自检通过
- 类型：手测 + 自检
- 结果：待手测

### TC-89 图片缩略图
- 对应验收：I1–I4（`docs/requirements/20260922-image-thumb.md`）
- 步骤：粘贴/发送 PNG；对端收图；再发一个非图片
- 期望：图片卡有缩略与 IMG 标记；非图片仍为 FILE；打开/目录/复制可用
- 类型：手测 + 自检
- 结果：待手测

### TC-90 拖到设备列表发送
- 对应验收：D1–D3（`docs/requirements/20260922-drop-on-peer.md`）
- 步骤：拖文件到未选中设备行；拖到列表空白；聊天区再拖一次
- 期望：落在行上则切会话并发送；空白有提示；聊天区仍可发当前会话
- 类型：手测
- 结果：待手测

### TC-91 最小化进托盘
- 对应验收：T1–T3（`docs/requirements/20260922-minimize-to-tray.md`）
- 步骤：默认设置点最小化；取消「关闭/最小化到托盘」后再最小化；托盘恢复
- 期望：开启时进托盘；关闭时进任务栏；托盘可恢复
- 类型：手测
- 结果：待手测

### TC-92 排队文件名预览
- 对应验收：Q1–Q3（`docs/requirements/20260922-queue-peek.md`）
- 步骤：发大文件再入队多个；悬停进度；清空排队
- 期望：进度出现下几个文件名；tooltip 有排队列表；清空后预览消失
- 类型：手测
- 结果：待手测

### TC-93 文件夹 zip 发送
- 对应验收：Z1–Z4（`docs/requirements/20260922-folder-zip.md`）
- 步骤：选含子目录文件夹；选打包 zip；再试仅发顶层；试仅有子目录的文件夹
- 期望：zip 含完整树；仅顶层行为旧版；仅子目录可 zip；失败有提示
- 类型：手测 + 自检
- 结果：待手测

### TC-94 设备置顶
- 对应验收：P1–P3（`docs/requirements/20260922-peer-pin.md`）
- 步骤：右键置顶；刷新/重启；取消置顶
- 期望：置顶最上且持久；取消后恢复排序；settings 有 pinnedPeers
- 类型：手测 + 自检
- 结果：待手测

### TC-95 接收取消
- 对应验收：R1–R3（`docs/requirements/20260922-cancel-recv.md`）
- 步骤：对端发大文件，本机接收中点取消；再测本机发送中的取消
- 期望：接收停且未完成文件删除、有「已取消」；发送取消仍只停发送
- 类型：手测
- 结果：待手测

### TC-96 拖放文件夹可选 zip
- 对应验收：D1–D4（`docs/requirements/20260922-drop-folder-zip.md`）
- 步骤：拖含子目录文件夹到聊天/列表；选 zip / 仅顶层 / 取消；再拖无嵌套文件夹
- 期望：嵌套可三选；无嵌套不弹框；取消不发
- 类型：手测
- 结果：待手测

### TC-97 打包异步可取消
- 对应验收：A1–A3（`docs/requirements/20260922-zip-async.md`）
- 步骤：打包较大目录时拖窗口；打包中点取消；再打包小目录发送
- 期望：不假死；取消不入队；小目录可发
- 类型：手测
- 结果：待手测

### TC-98 托盘发送完成提示
- 对应验收：S1–S3（`docs/requirements/20260922-send-done-tray.md`）
- 步骤：最小化到托盘后发完文件；前台再发一次；取消发送
- 期望：托盘有完成提示；前台不弹；取消无完成提示
- 类型：手测
- 结果：待手测

### TC-99 多网卡优选本机 IP
- 对应验收：N1–N3（`docs/requirements/20260922-preferred-ip.md`）
- 步骤：设置选非自动 IP；看顶栏与复制；开共享看 URL；重启
- 期望：展示与复制一致；共享 URL 用所选 IP；重启保持
- 类型：手测 + 自检
- 结果：待手测

### TC-100 清理临时 zip
- 对应验收：C1–C3（`docs/requirements/20260922-cleanup-temp-zip.md`）
- 步骤：zip 发成功；再 zip 发送中取消；发普通文件
- 期望：临时 zip 删除；普通文件不受影响
- 类型：手测 + 自检
- 结果：待手测

### TC-101 标题栏置顶
- 对应验收：P1–P3（`docs/requirements/20260922-title-pin.md`）
- 步骤：点顶栏钉；再点取消；重启
- 期望：置顶/取消立刻生效；重启保持
- 类型：手测
- 结果：待手测

### TC-102 自动同网段本机 IP
- 对应验收：A1–A3（`docs/requirements/20260922-auto-same-subnet-ip.md`）
- 步骤：设置本机 IP 为自动；切换不同网段对端看顶栏
- 期望：尽量跟对端同段；手动指定不跟；无匹配仍有地址
- 类型：手测 + 自检
- 结果：待手测

### TC-103 默认窗口紧凑且聊天无横向滚动条
- 对应验收：W1–W3（`docs/requirements/20260922-compact-window-no-hscroll.md`）
- 步骤：清除记忆几何后启动看默认大小；选对端发短消息看聊天区；再缩小窗口看是否仅纵向滚
- 期望：默认约 960×640 量级、明显小于整屏；短消息无横向条；窄窗内容换行、不靠横向条阅读
- 类型：手测
- 结果：待手测

### TC-104 气泡形状放大对齐参考图
- 对应验收：S1–S3（`docs/requirements/20260922-bubble-shape-scale.md`）
- 步骤：选对端发短消息与长换行；对照参考图看圆角与字号；切会话再回来
- 期望：圆角柔和无尾巴；字与气泡明显更大；配色与左右对齐不变、历史重绘正确
- 类型：手测
- 结果：待手测

### TC-105 默认更紧凑且可边缘缩放
- 对应验收：R1–R4（`docs/requirements/20260922-window-resize-compact.md`）
- 步骤：清除几何后启动看默认大小；非最大化拖四边/四角；最大化时拖边缘；调小后 ✕ 进托盘再开
- 期望：默认约 880×560；可拖边改大小且不低于最小；最大化边缘无效；再开尺寸保持
- 类型：手测
- 结果：待手测

### TC-106 顶栏图标视觉尺寸一致
- 对应验收：I1–I2（`docs/requirements/20260922-title-icon-uniform.md`）
- 步骤：看顶栏下载目录、设置、置顶、最小化、最大化、关闭一排图标
- 期望：目视大小与线重接近，无个别明显偏大/偏小；点击功能正常
- 类型：手测
- 结果：待手测

### TC-107 气泡「复制」链接可读
- 对应验收：V1–V2（`docs/requirements/20260922-copy-link-contrast.md`）
- 步骤：发一条短文字，看气泡下「复制」；点击一次
- 期望：浅蓝底上「复制」清楚可读；剪贴板为原文
- 类型：手测
- 结果：待手测

### TC-108 文字乐观发送
- 对应验收：O1–O4（`docs/requirements/20260922-optimistic-text-send.md`）
- 步骤：选对端回车发一句；看气泡与输入框；等送达勾；再对不可达对端发一句看失败
- 期望：立刻出气泡且输入清空；成功有已送达；失败无假成功气泡且有失败条；慢网连按不因未清空重复同一句
- 类型：手测
- 结果：待手测

### TC-109 空会话引导提示
- 对应验收：E1–E2（`docs/requirements/20260922-empty-chat-hint.md`）
- 步骤：选尚无聊天记录的对端；再发一条字
- 期望：先见「发消息，或把文件拖到这里」；有往来后消失
- 类型：手测
- 结果：待手测

### TC-110 输入区快捷键改悬停且 Esc 清空
- 对应验收：H1–H3（`docs/requirements/20260922-composer-hint-esc.md`）
- 步骤：看工具行无常驻键帽；悬停输入框；输入框打字后按 Esc；再试 Enter / Shift+Enter
- 期望：无键帽条；悬停有说明；Esc 清空草稿；发送/换行行为不变
- 类型：手测
- 结果：待手测

### TC-111 发送进度与气泡去重
- 对应验收：D1–D3（`docs/requirements/20260922-progress-dedupe.md`）
- 步骤：发一个大文件看气泡与顶栏；再排队 2 个文件看顶栏；对方发大文件看接收顶栏
- 期望：气泡有 N%，发/收顶栏无重复「文件名 N%」；有排队时仍见剩余数；取消可用
- 类型：手测
- 结果：待手测

### TC-112 空输入发送钮灰显
- 对应验收：S1–S3（`docs/requirements/20260922-send-btn-empty.md`）
- 步骤：选对端看空输入时发送钮；打字；清空；取消选中
- 期望：空/未选对端时灰不可点；有字可点且 Enter 可发
- 类型：手测
- 结果：待手测

### TC-113 未选对端空态三步说明
- 对应验收：N1–N2（`docs/requirements/20260922-empty-peer-steps.md`）
- 步骤：无选中时看右侧；搜索无结果时再看
- 期望：默认空态含加 IP 与发消息/拖文件步骤；搜索无结果不套用三步
- 类型：手测
- 结果：待手测

### TC-114 离线对端一眼可辨
- 对应验收：L1–L3（`docs/requirements/20260922-offline-peer-visual.md`）
- 步骤：列表对比在线/离线色；选中离线看顶栏与胶囊；对端上线后再看
- 期望：离线列表更淡；顶栏含「离线」；胶囊为「对方当前离线」；上线后恢复直连文案
- 类型：手测
- 结果：待手测

### TC-115 文件卡目录/路径链接可读
- 对应验收：F1–F2（`docs/requirements/20260922-file-card-links.md`）
- 步骤：看完成态文件卡三处链接并各点一次
- 期望：打开文件/目录/复制路径均清楚可点；行为与改前一致
- 类型：手测
- 结果：待手测

### TC-116 抖动即时反馈
- 对应验收：N1–N3（`docs/requirements/20260922-nudge-feedback.md`）
- 步骤：选对端点抖动；再对离线对端点一次
- 期望：立刻出现「正在发送抖动」；成功有系统条并本机轻抖；离线有轻提示
- 类型：手测
- 结果：待手测

### TC-117 点聊天聚焦与未读醒目
- 对应验收：C1–C3（`docs/requirements/20260922-chat-focus-unread.md`）
- 步骤：点聊天空白处打字；点「复制」；另一对端收消息看列表
- 期望：点空白可输入；链接仍可用；有「未读 N」且加粗，点入后消失
- 类型：手测
- 结果：待手测

### TC-118 粘贴截图有反馈
- 对应验收：P1–P2（`docs/requirements/20260922-paste-image-feedback.md`）
- 步骤：截图后 Ctrl+V；再复制文件 Ctrl+V
- 期望：截图出现「已粘贴截图…」系统条并开始发送；复制文件不出现该条
- 类型：手测
- 结果：待手测

### TC-119 设置聚焦与共享中高亮
- 对应验收：S1–S3（`docs/requirements/20260922-settings-focus-share-glow.md`）
- 步骤：打开设置直接改名；开启网页共享看顶栏；关闭共享再看
- 期望：打开即能改设备名；共享中按钮绿色系；关闭后恢复蓝底
- 类型：手测
- 结果：待手测

### TC-120 取消全部与清空排队易区分
- 对应验收：A1–A3（`docs/requirements/20260922-cancel-clear-labels.md`）
- 步骤：发送中再入队，看按钮文案与悬停；分别点清空排队、取消全部
- 期望：文案为「取消全部」「清空排队」；清空后当前继续；取消全部则全停
- 类型：手测
- 结果：待手测

### TC-121 设备右键复制地址与发送文件
- 对应验收：M1–M3（`docs/requirements/20260922-peer-context-actions.md`）
- 步骤：对列表设备右键；复制地址；再右键发送文件
- 期望：菜单有两项；剪贴板正确且有提示；文件发给该设备
- 类型：手测
- 结果：待手测

### TC-122 对端上线离线切换提示
- 对应验收：U1–U3（`docs/requirements/20260922-peer-online-transition.md`）
- 步骤：选中某设备；对方关/开局域快传；观察聊天系统条；刚切入会话时不应误报
- 期望：离线→在线有上线提示；在线→离线有离线提示；首次进入无切换提示
- 类型：手测
- 结果：待手测

### TC-123 文字失败可重发
- 对应验收：T1–T3（`docs/requirements/20260922-text-fail-resend.md`）
- 步骤：对端不可达时发文字；点「重发」；输入框预先打一句草稿再点重发
- 期望：失败条有「重发」；点后出现乐观气泡；草稿仍在
- 类型：手测
- 结果：待手测

### TC-124 会话顶栏打开下载目录
- 对应验收：D1–D2（`docs/requirements/20260922-session-open-download.md`）
- 步骤：选中对端，点会话顶栏「下载目录」
- 期望：打开设置中的接收目录
- 类型：手测
- 结果：待手测

### TC-125 文件卡点文件名打开
- 对应验收：F1–F3（`docs/requirements/20260922-file-card-open-by-name.md`）
- 步骤：收/发完成一张非图片文件卡；点文件名；再点「打开所在目录」
- 期望：点文件名打开文件；传输中不可开；目录链接仍可用
- 类型：手测
- 结果：待手测

### TC-126 开机自动启动
- 对应验收：A1–A3（`docs/requirements/20260922-run-at-startup.md`）
- 步骤：设置开启并保存；查任务管理器「启动应用」或注册表 Run；再关闭并保存
- 期望：默认关；开启后出现启动项；关闭后消失
- 类型：手测
- 结果：待手测

### TC-127 文件卡圆角位图与进度条
- 对应验收：V1–V3（`docs/requirements/20260922-file-card-modern.md`）
- 步骤：发送/接收文件，观察传输中与完成态；点卡面打开；点打开所在目录
- 期望：圆角卡与圆角进度；点卡面可开；目录链可用
- 类型：手测
- 结果：待手测

### TC-128 侧栏与发送钮现代观感
- 对应验收：L1–L3（`docs/requirements/20260922-sidebar-send-modern.md`）
- 步骤：看列表选中/未读角标；输入区空/有字时看发送钮
- 期望：选中有左侧蓝条；未读在头像角；发送钮圆形主色，空内容灰
- 类型：手测
- 结果：待手测

### TC-129 气泡轻阴影
- 对应验收：S1–S2（`docs/requirements/20260922-bubble-soft-shadow.md`）
- 步骤：发文字、看文件卡与系统提示
- 期望：气泡/文件卡/系统条相对灰底略有浮起感
- 类型：手测
- 结果：待手测

### TC-130 空态白卡与输入字号
- 对应验收：E1–E3（`docs/requirements/20260922-empty-card-input-type.md`）
- 步骤：未选对端看空态；搜索无结果；看输入区字号
- 期望：居中白卡分层；搜索无结果同风格；输入字更大
- 类型：手测
- 结果：待手测

### TC-131 传输进度胶囊
- 对应验收：P1–P2（`docs/requirements/20260922-progress-capsule.md`）
- 步骤：发/收文件，看输入区上方与文件 Tab 顶栏；点清空排队/取消全部
- 期望：进度行呈浅蓝圆角胶囊；按钮仍在同行可用
- 类型：手测
- 结果：待手测

### TC-132 文件 Tab 空态白卡
- 对应验收：F1–F2（`docs/requirements/20260922-files-tab-empty.md`）
- 步骤：选对端切「文件传输」且无历史；再传一个文件后查看
- 期望：空时白卡引导；有记录后列出文件卡
- 类型：手测
- 结果：待手测

### TC-133 会话 Tab 分段控件
- 对应验收：T1–T2（`docs/requirements/20260922-session-tab-segment.md`）
- 步骤：选对端，在「即时聊天 / 文件传输」间切换
- 期望：两 Tab 同处灰底槽；选中项白底浮起
- 类型：手测
- 结果：待手测

### TC-134 输入与搜索聚焦描边
- 对应验收：F1–F3（`docs/requirements/20260922-focus-ring.md`）
- 步骤：点输入框；点别处；点侧栏搜索
- 期望：聚焦蓝边；失焦恢复；搜索同样有 focus 描边
- 类型：手测
- 结果：待手测

### TC-135 细滚动条与在线折叠连接条
- 对应验收：R1–R4（`docs/requirements/20260922-scrollbar-banner-fold.md`）
- 步骤：看侧栏/聊天滚动条；选在线对端；再选或模拟离线对端；看顶栏在线/离线色
- 期望：滚动条细圆角；在线无连接条；离线有警示条；在线绿/离线琥珀
- 类型：手测
- 结果：待手测

### TC-136 Composer 工具行分段槽
- 对应验收：C1–C2（`docs/requirements/20260922-composer-tool-bar.md`）
- 步骤：选对端，看输入区上方三按钮；悬停发送文件
- 期望：三按钮同处灰底槽；悬停白底浮起
- 类型：手测
- 结果：待手测

### TC-137 聊天空会话白卡
- 对应验收：E1–E2（`docs/requirements/20260922-empty-chat-card.md`）
- 步骤：选对端且无聊天记录；发一条文字后再看
- 期望：空时居中白卡引导；有消息后消失
- 类型：手测
- 结果：待手测

### TC-138 空卡阴影与拖放遮罩图标
- 对应验收：D1–D2（`docs/requirements/20260922-empty-shadow-drop-icon.md`）
- 步骤：未选对端看空卡；向聊天区拖文件看遮罩
- 期望：空卡有轻阴影；遮罩有图标与「松开即可发送」
- 类型：手测
- 结果：待手测

### TC-139 弹窗阴影与点气泡复制
- 对应验收：P1–P3（`docs/requirements/20260922-dialog-shadow-bubble-copy.md`）
- 步骤：打开设置/共享/加 IP；点文字气泡；再点「复制」链
- 期望：弹窗有轻阴影；点气泡可复制并提示；链仍可用
- 类型：手测
- 结果：待手测

### TC-140 复制提示与设置下拉对齐
- 对应验收：A1–A3（`docs/requirements/20260922-copy-affordance-combo.md`）
- 步骤：悬停文字气泡；打开设置看本机展示 IP 下拉；打开共享看滚动条
- 期望：悬停提示「点击复制」；下拉圆角现代；共享滚动条细
- 类型：手测
- 结果：待手测

### TC-141 搜索快捷键与列表拖放遮罩
- 对应验收：S1–S3（`docs/requirements/20260922-search-hint-drop-list.md`）
- 步骤：看搜索占位；悬停文件卡复制路径；向侧栏设备列表拖文件
- 期望：占位含 Ctrl+F；悬停有提示；侧栏出现「拖到具体设备上…」遮罩
- 类型：手测
- 结果：待手测

### TC-142 离线条暖色与侧栏空态
- 对应验收：O1–O4（`docs/requirements/20260922-offline-empty-list-hints.md`）
- 步骤：选离线对端看连接条；侧栏 0 设备看列表空态与角标；悬停搜索/输入看 tooltip；再向侧栏拖文件
- 期望：离线条琥珀底+警示三角；空态有引导且角标灰；tooltip 含 Esc/Ctrl+V；拖放遮罩有图标与副文案
- 类型：手测
- 结果：待手测

### TC-143 白卡确认与提示弹窗
- 对应验收：D1–D3（`docs/requirements/20260922-app-dialogs-card.md`）
- 步骤：清空会话；删除手动节点；发送含二级目录的文件夹；设置里填无效端口保存
- 期望：均为圆角白卡+阴影，非系统 MessageBox；确认默认焦点在取消
- 类型：手测
- 结果：待手测

### TC-144 输入壳浮起与顶栏减负
- 对应验收：H1–H4（`docs/requirements/20260922-composer-header-depth.md`）
- 步骤：看输入壳与侧栏分隔；悬停会话顶栏文件夹图标；选离线对端看连接条；发送文件看进度胶囊
- 期望：输入壳/离线条/进度有轻阴影；下载为图标+tooltip；侧栏有极淡右边线
- 类型：手测
- 结果：待手测

### TC-64 并发线程数诚实化
- 对应验收：H1–H3（`docs/requirements/20260922-threads-honest.md`）
- 步骤：打开设置看线程数不可改与说明；改设备名保存；再看 settings.json 的 transferThreads 未因本操作乱变
- 期望：框禁用且有单队列说明；其它项可保存；上传仍单文件排队
- 类型：手测
- 结果：待手测

### TC-38 设置下载目录浏览
- 对应验收：B1–B3（`docs/requirements/20260922-settings-browse-dir.md`）
- 步骤：打开设置 → 点「浏览…」选一文件夹 → 确认 → 再点浏览后取消 → 保存并关闭
- 期望：确认后路径写入输入框；取消不改；保存后新目录生效
- 类型：手测
- 结果：待手测

### TC-39 系统托盘常驻
- 对应验收：T1–T3（`docs/requirements/20260922-system-tray.md`）
- 步骤：点标题栏 ✕ → 看托盘 → 单击托盘或「显示主窗口」→ 再 ✕ → 托盘右键「退出局域快传」
- 期望：✕ 后窗口隐藏进程仍在、可收文件；可恢复窗口；退出后进程结束
- 类型：手测
- 结果：待手测

### TC-40 聊天区拖放发送
- 对应验收：D1–D4（`docs/requirements/20260922-chat-drop-send.md`）
- 步骤：选中对端 → 拖一个文件进聊天区；再拖含多文件的文件夹；未选对端时拖一下
- 期望：单文件发出；文件夹顶层文件依次发；未选对端有提示；发送按钮行为不变
- 类型：手测
- 结果：待手测

### TC-40b 聊天区 Ctrl+V 粘贴发送
- 对应验收：P1–P4（`docs/requirements/20260922-chat-paste-send.md`）
- 步骤：选中对端 → 资源管理器复制文件 → 输入框 Ctrl+V；再粘贴普通文字；未选对端时复制文件再 Ctrl+V
- 期望：文件发出；纯文字仍粘贴进输入框；未选对端有提示；拖放仍可用
- 类型：手测
- 结果：待手测

### TC-41 打开下载目录
- 对应验收：O1–O4（`docs/requirements/20260922-open-download-dir.md`）
- 步骤：点顶栏文件夹图标；再托盘右键「打开下载目录」
- 期望：均打开当前设置中的下载目录；目录不存在时会先创建
- 类型：手测
- 结果：待手测

### TC-42 后台收件托盘气泡
- 对应验收：N1–N5（`docs/requirements/20260922-tray-notify.md`）；J1–J4（`docs/requirements/20260922-tray-jump-peer.md`）
- 步骤：点 ✕ 进托盘 → 首次提示可点（不切会话）→ 对端发文字与文件 → 点收件提示；再仅点托盘图标恢复
- 期望：收件提示含发送方/文件名；点提示恢复并选中该对端、聊天滚到最新；仅点图标不改选中；前台不弹
- 类型：手测
- 结果：待手测

### TC-44 对端未读角标
- 对应验收：U1–U4（`docs/requirements/20260922-unread-badge.md`）
- 步骤：选中 A → B 发文字；点 B；再隐藏窗口让当前对端发文件后点托盘图标恢复；另测抖动不计未读
- 期望：B 名旁出现 `· N`；点进后消失；托盘恢复清当前未读；抖动不加未读
- 类型：手测
- 结果：待手测

### TC-43 ARM 麒麟中文输入
- 对应验收：A1–A3（`docs/requirements/20260922-kylin-ime-chinese.md`）
- 步骤：麒麟上用 `landrop.sh` 启动 → 聊天框切中文输入法打字 → 设置改中文设备名
- 期望：可出候选并上屏；进程有 `QT_IM_MODULE=fcitx`；`plugins/platforminputcontexts/` 能解析到 fcitx 插件
- 类型：手测（22 机）
- 结果：待手测

### TC-45 发送侧气泡内进度
- 对应验收：P1–P5（`docs/requirements/20260922-bubble-send-progress.md`）
- 步骤：选中对端发一个较大文件；观察聊天与「文件传输」Tab；传完后点打开；另测断网或关对端触发失败；传输中切到其他对端再切回
- 期望：立刻出现「发送中 N%」且条未满；完成后同卡满条+「传输完成」+可打开；失败无假完成卡；切回仍见正确进度
- 类型：手测
- 结果：待手测

### TC-46 文件发送失败重试
- 对应验收：R1–R4（`docs/requirements/20260922-send-fail-retry.md`）
- 步骤：关对端或断网后发文件 → 看失败条「重试」→ 恢复对端后点重试；另在上传中点旧失败的重试；再删源文件后点重试
- 期望：失败条有「重试」；恢复后重试可传成；上传中点重试有进度区「请稍候」类提示且不弹模态、不打断；源文件没了有失败提示不崩
- 类型：手测
- 结果：待手测

### TC-47 聊天拖放虚线反馈
- 对应验收：V1–V4（`docs/requirements/20260922-chat-drop-visual.md`）
- 步骤：选中对端 → 拖文件进入会话区（聊天/输入框之间移动）→ 拖出 → 再拖入松手发送
- 期望：进入出现蓝虚线与「松手发送到 …」；区内移动不闪灭；拖出或松手后消失；文件仍能发出
- 类型：手测
- 结果：待手测

### TC-48 聊天贴底滚动
- 对应验收：S1–S4（`docs/requirements/20260922-chat-stick-bottom.md`）
- 步骤：选中对端发较大文件 → 上传中上翻看历史；再滚回底部附近继续传；再切换对端后切回
- 期望：上翻时进度仍更新但视口不被拽回；贴底时自动跟最新；切回会话落在最新附近；短会话无滚动条不崩
- 类型：手测
- 结果：待手测

### TC-49 窗口位置与大小记忆
- 对应验收：G1–G4（`docs/requirements/20260922-window-geometry.md`）
- 步骤：拉大并移动窗口 → ✕ 进托盘再打开；再最大化后进托盘再打开；`--self-check`
- 期望：普通尺寸/位置恢复；最大化可恢复；自检含 window geom；缺字段启动不崩
- 类型：手测 / 集成
- 结果：`--self-check` 含 window geom；界面待手测

### TC-50 有新消息回到底部
- 对应验收：J1–J4（`docs/requirements/20260922-jump-bottom.md`）
- 步骤：选中对端上翻 → 对端发文字；再上翻后本机发文件至完成；上翻只看发送进度；手动滚回底部；切换对端
- 期望：文字/文件完成出现「有新消息 ↓」可点回底部；进度 tick 不刷按钮；贴底或切会话后按钮消失
- 类型：手测
- 结果：待手测

### TC-51 一键复制本机地址
- 对应验收：C1–C4（`docs/requirements/20260922-copy-local-addr.md`）
- 步骤：点击顶栏本机胶囊 → 粘贴到记事本；改传输端口保存后再点复制；拖窗口时勿点在胶囊上误测
- 期望：剪贴板为 `IP:端口`；有「已复制」提示；点击胶囊不拖窗；改端口后复制含新端口
- 类型：手测
- 结果：待手测

### TC-52 左侧栏宽度记忆
- 对应验收：W1–W4（`docs/requirements/20260922-side-width.md`）
- 步骤：拖分割条拉宽侧栏 → ✕ 进托盘再打开；再试拖到极限；`--self-check`
- 期望：宽度恢复；约 220–480 限制；缺字段默认约 300；自检含 sideWidth
- 类型：手测 / 集成
- 结果：`--self-check` 含 sideWidth；界面待手测

### TC-53 记住上次选中的对端
- 对应验收：L1–L4（`docs/requirements/20260922-last-peer.md`）
- 步骤：选中对端 B → 退出再开；删掉/离线使 B 不在列表后再开；运行中改选 A 后等列表刷新；`--self-check`
- 期望：B 仍在则自动选中；不在则回退不崩；刷新不把 A 打回旧 lastPeer；自检含 lastPeer
- 类型：手测 / 集成
- 结果：`--self-check` 含 lastPeer；界面待手测

### TC-54 会话顶栏复制对端地址
- 对应验收：P1–P4（`docs/requirements/20260922-copy-peer-addr.md`）
- 步骤：选中对端 A → 点顶栏地址粘贴核对；再选对端 B 再点；再点本机胶囊确认互不干扰
- 期望：剪贴板分别为 A、B 的 `IP:端口`；有「已复制」提示；本机复制仍正常
- 类型：手测
- 结果：待手测

### TC-55 聊天记录落盘
- 对应验收：H1–H5（`docs/requirements/20260922-chat-persist.md`）
- 步骤：与对端互发文字与文件 → ✕ 进托盘或退出再开；另测无 chat.json / 损坏文件启动；`--self-check`
- 期望：历史气泡与完成文件卡仍在；损坏不崩；自检含 chat 往返且跳过进行中文件卡
- 类型：手测 / 集成
- 结果：`--self-check` 含 chat 往返；界面待手测

### TC-56 清空当前会话聊天记录
- 对应验收：X1–X5（`docs/requirements/20260922-clear-chat.md`）
- 步骤：选对端有若干气泡 → 右键「清空聊天记录」→ 取消一次仍在；再确认清空；退出再开；另测发送中清空应提示；确认下载目录文件仍在、手动节点仍在
- 期望：确认后聊天/文件 Tab 空；重启仍空；取消无变；发送中不清；节点与落盘文件保留
- 类型：手测
- 结果：待手测

### TC-57 接收方气泡进度
- 对应验收：R1–R5（`docs/requirements/20260922-recv-progress.md`）
- 步骤：对端发较大文件 → 本机聊天出现「接收中 N%」→ 完成后可打开；退出再开确认进行中未残留、完成卡仍在；`--self-check`
- 期望：完成前有进度；完成后可开；chat.json 不存进行中 InFile；自检跳过接收中卡
- 类型：手测 / 集成
- 结果：`--self-check` 含跳过接收中；界面待手测

### TC-61 接收失败可见提示
- 对应验收：F1–F4（`docs/requirements/20260922-recv-fail-notice.md`）
- 步骤：对端开始发大文件 → 本机出现接收中 → 中断对端发送；看失败条；关通知声再测；退出再开
- 期望：进度卡消失、红色失败条含文件名、无重试链；开时有声关时无；失败条落盘仍在
- 类型：手测
- 结果：待手测

### TC-22 设置保存与 Nudge 开关
- 对应验收：S4、S5
- 步骤：改本机名称并保存；再关闭 Nudge 后点「抖动窗口」
- 期望：顶栏本机名称更新；关闭 Nudge 后不发送抖动
- 类型：手测
- 结果：待手测

### TC-23 设置开关勾选对号
- 对应验收：S7
- 步骤：打开设置，看两项开关勾选态；再取消勾选
- 期望：勾选时蓝框内有白色对号；取消后为空框
- 类型：手测
- 结果：待目视确认

### TC-24 网页共享对话框布局
- 对应验收：W1、W2、W3、W6（`docs/requirements/20260921-web-share-dialog.md`）
- 步骤：点顶栏「网页共享」
- 期望：标题「本机 HTTP 网页共享服务」、副标题、蓝色分享徽标、运行/暂停状态；左卡地址含 `/share/` 与「复制网址」「本机浏览器自测」；右卡二维码与左卡等高；四角无黑三角
- 类型：手测
- 结果：待目视确认

### TC-25 网页共享列表与暂停
- 对应验收：W4、W5
- 步骤：点「添加文件到共享」选一个含顶层文件的目录；再点「暂停 HTTP 服务」；再点「开启 HTTP 服务」
- 期望：列表出现文件名与大小且数量一致；暂停后列表为空、按钮变为开启；再次开启后列表恢复
- 类型：手测
- 结果：待手测

### TC-26 网页共享二维码
- 对应验收：W3
- 步骤：运行 `landrop --self-check`（或既有自检入口）
- 期望：自检通过；二维码对 `http://192.168.1.108:8848/share/` 生成，且左上、右上定位角为深色模块
- 类型：集成
- 结果：通过（`--self-check` 退出码 0）

### TC-27 网页共享对话框图标
- 对应验收：W7（`docs/requirements/20260921-web-share-dialog.md`）
- 步骤：打开网页共享对话框，看左上徽标与底栏「暂停/开启 HTTP 服务」按钮
- 期望：徽标为蓝底圆角 + 白色三节点分享连线；按钮左侧为深灰电源开关符号
- 类型：手测
- 结果：待目视确认

### TC-28 直连胶囊
- 对应验收：M1（`docs/requirements/20260921-chat-message-bubbles.md`）；C1–C4（`docs/requirements/20260922-conn-banner-capsule.md`）
- 步骤：选中一台在线对端，看会话顶栏下方状态条
- 期望：浅灰底上居中白底真胶囊（细灰边、两端半圆）；绿盾；「已建立局域网直连：」灰字 + 设备名加粗深色 + `(IP:端口)` 灰字
- 类型：手测
- 结果：待目视确认

### TC-29 聊天气泡与送达
- 对应验收：M2、M3、M7；B1–B3（`docs/requirements/20260922-chat-avatar-bubble.md`）
- 步骤：向对端发一条文字；对端回一条；看头像与气泡外形；切换另一台再切回来
- 期望：头像约 40px 圆角方；发出为右蓝圆角矩形气泡「我 + 时间 + ✓✓ 已送达 - Nms」；收到为左白圆角矩形气泡；切回后历史仍正确
- 类型：手测
- 结果：待手测

### TC-30 文件卡与代码块
- 对应验收：M4、M5、M6；F1–F4（`docs/requirements/20260922-file-card-open.md`）
- 步骤：收一个文件 → 点「打开文件」与「打开所在目录」；再发一段以 ```bash 开头的代码围栏；点抖动
- 期望：打开文件用默认程序打开该文件；打开所在目录进父目录；代码块深色且「复制」可用；抖动为居中提示
- 类型：手测
- 结果：待手测

### TC-31 会话 Ping 与链路
- 对应验收：P1、P2、P4（`docs/requirements/20260921-ping-and-files.md`）
- 步骤：选中在线对端，看顶栏副行；再换一台对端
- 期望：几秒内出现 `Ping` 毫秒或「超时」，以及本机网卡速率（如千兆/2.5 GbE 网线）；换对端后时延跟着变，不沿用上一台
- 类型：手测
- 结果：待手测

### TC-32 文件传输列表
- 对应验收：P3
- 步骤：向当前对端发送一个文件，再打开「文件传输」
- 期望：按钮数字大于 0，页内能看到该文件名与大小；进行中时页顶有进度文字
- 类型：手测
- 结果：待手测

### TC-33 通知声开关
- 对应验收：N1–N4（`docs/requirements/20260922-sound-notification.md`）
- 步骤：保持通知声开启，对端发文字与文件；再关掉通知声保存后重发；再发一次抖动
- 期望：开时收文字/文件各有一声系统提示；关后无声；抖动不额外 beep
- 类型：手测
- 结果：待手测

### TC-59 发送文件完成通知声
- 对应验收：S1–S4（`docs/requirements/20260922-send-complete-sound.md`）
- 步骤：通知声开 → 本机发文件至完成听一声；关开关保存后再发完成；另发一条文字确认无额外声
- 期望：开时发文件完成有一声；关后无声；发文字成功不因本功能多播；收件行为不变
- 类型：手测
- 结果：待手测

### TC-63 自定义通知铃声
- 对应验收：C1–C5（`docs/requirements/20260922-custom-ringtone.md`）
- 步骤：设置选 wav → 试听 → 保存 → 收/发文件听铃声；恢复默认再测；关通知声；无效路径；`--self-check`
- 期望：有效 wav 生效；默认回系统音；关开关收发静音但试听可用；无效回落不崩；自检含 soundFile
- 类型：手测 / 集成
- 结果：`--self-check` 含 soundFile；界面待手测

### TC-34 会话顶栏主机名
- 对应验收：H1–H3（`docs/requirements/20260922-peer-hostname.md`）
- 步骤：本机 `landrop --self-check`；选中对端看副行首段；可用浏览器打开 `http://本机IP:端口/api/info` 看 `hostname`
- 期望：自检含 hostname；`/api/info` 有 hostname 字段；副行为「主机名 · Ping · 链路」，无字段时回退设备名且不崩
- 类型：手测 / 集成
- 结果：自检通过（`--self-check` 含 hostname）；界面待双机目视

### TC-35 网页共享添加文件与拖放
- 对应验收：F1–F5、W8（`docs/requirements/20260922-share-add-files.md`）
- 步骤：打开网页共享 →「添加文件」多选；再拖一个文件到虚线区；同名再加一次；点行内删除；浏览器打开 `/share/`
- 期望：列表与数量同步；同名出现后缀文件；删除后列表与下载页无该文件；源文件仍在；`--self-check` 含复制去重
- 类型：手测 / 集成
- 结果：自检通过；界面待手测

### TC-36 顶栏品牌图标与文字高度对齐
- 对应验收：U9（`docs/requirements/20260321-ui-icons.md`）
- 步骤：启动后看左上蓝图标与「局域快传 / 在线 · …」两行
- 期望：文字块高度与图标同为约 36px，整体垂直居中；标题顶边与图标顶边齐，状态行底边与图标底边齐
- 类型：手测
- 结果：待目视确认

### 缺口
- 扫码：对话框已出二维码；旧条目 A9 仅复制链接的说法以 W3 为准
- 网页共享添加文件/拖放/行内删除：见 `20260922-share-add-files.md`（本轮已落地）
- 聊天气泡、文件卡片、Ping/链路/传输 Tab、hostname：已落地，见对应需求文档
- 手动节点持久化：见 `20260922-manual-peers-persist.md`（本轮已落地）
- 手动节点删除：见 `20260922-remove-manual-peer.md`（本轮已落地）
- 部门标签入库：见 `20260922-peer-tag.md`（本轮已落地）
- 右键编辑手动节点别名/标签：见 `20260922-edit-manual-peer.md`（本轮已落地）
- 自定义铃声：见 `20260922-custom-ringtone.md`（本轮已落地）
- 并发线程数诚实化：见 `20260922-threads-honest.md`（本轮已落地）
- 通知声（收件）：见 `20260922-sound-notification.md`
- 发送完成通知声：见 `20260922-send-complete-sound.md`（本轮已落地）
- 真并发上传线程池：仍非本轮（大项）
- 设置下载目录浏览按钮：见 `20260922-settings-browse-dir.md`（本轮已落地）
- 系统托盘常驻：见 `20260922-system-tray.md`（本轮已落地）
- 聊天区拖放发送：见 `20260922-chat-drop-send.md`（本轮已落地）
- 打开下载目录：见 `20260922-open-download-dir.md`（本轮已落地）
- 后台收件托盘气泡：见 `20260922-tray-notify.md`（本轮已落地）
- 气泡内发送进度：见 `20260922-bubble-send-progress.md`（本轮已落地）
- 文件发送失败重试：见 `20260922-send-fail-retry.md`（本轮已落地）
- 聊天拖放虚线反馈：见 `20260922-chat-drop-visual.md`（本轮已落地）
- 聊天贴底滚动：见 `20260922-chat-stick-bottom.md`（本轮已落地）
- 窗口位置与大小记忆：见 `20260922-window-geometry.md`（本轮已落地）
- 有新消息回到底部：见 `20260922-jump-bottom.md`（本轮已落地）
- 一键复制本机地址：见 `20260922-copy-local-addr.md`（本轮已落地）
- 左侧栏宽度记忆：见 `20260922-side-width.md`（本轮已落地）
- 记住上次选中对端：见 `20260922-last-peer.md`（本轮已落地）
- 会话顶栏复制对端地址：见 `20260922-copy-peer-addr.md`（本轮已落地）
- 聊天记录落盘：见 `20260922-chat-persist.md`（本轮已落地）
- 清空当前会话聊天记录：见 `20260922-clear-chat.md`（本轮已落地）
- 接收方气泡进度：见 `20260922-recv-progress.md`（本轮已落地）
- 接收失败可见提示：见 `20260922-recv-fail-notice.md`（本轮已落地）
- 发文件夹仅顶层说明：见 `20260922-folder-top-only-hint.md`（本轮已落地）
- 设置副标题与搜索空态：见 `20260922-copy-honest-search.md`（本轮已落地）
- 上传中非打断提示：见 `20260922-busy-upload-soft.md`（本轮已落地）
- 发送中自动入队：见 `20260922-upload-queue-append.md`（本轮已落地）
- 取消当前发送：见 `20260922-cancel-upload.md`（本轮已落地）
- 进度显示排队剩余：见 `20260922-upload-queue-remain.md`（本轮已落地）
- 发送文件多选：见 `20260922-send-file-multiselect.md`（本轮已落地）
- 粘贴剪贴板图片发送：见 `20260922-paste-clipboard-image.md`（本轮已落地）
- 打开所在目录并选中：见 `20260922-reveal-select.md`（本轮已落地）
- 选中对端聚焦输入框：见 `20260922-focus-input-on-peer.md`（本轮已落地）
- 搜索清空与 Ctrl+F：见 `20260922-search-clear-focus.md`（本轮已落地）
- 离线发送轻提示：见 `20260922-offline-send-hint.md`（本轮已落地）
- 文件卡复制路径：见 `20260922-copy-file-path.md`（本轮已落地）
- 文字气泡复制：见 `20260922-text-bubble-copy.md`（本轮已落地）
- 失败后重发剩余队列：见 `20260922-retry-remaining-queue.md`（本轮已落地）
- 发送进度速率：见 `20260922-upload-speed.md`（本轮已落地）
- 关闭进托盘可配置：见 `20260922-close-to-tray-option.md`（本轮已落地）
- 复制成功提示：见 `20260922-copy-feedback.md`（本轮已落地）
- 接收进度与速率：见 `20260922-recv-speed.md`（本轮已落地）
- 清空发送排队：见 `20260922-clear-upload-queue.md`（本轮已落地）
- 仓库里的 Go 测试（`go test ./...`）测的是旧 Go 进程，不能代替 Qt 客户端
