# MVP 验收用例

对应需求：`docs/requirements/20260320-局域快传-mvp.md`  
当前交付物是 Qt 客户端，不是 Go 里那套网页接口。下面标的是 2026-09-21 在本机对 Qt 包的执行结果。

### TC-01 文本收发与中文
- 对应验收：A3
- 前置：Windows 包 `dist/windows-x64/landrop.exe` 已启动
- 步骤：向 `POST /api/inbox` 投递 UTF-8 中文
- 期望：接口返回成功，对端聊天区能看到 UTF-8 中文
- 类型：集成
- 自动化：`powershell -File tests\api-smoke.ps1`
- 结果：通过。2026-09-21 Windows（omen / 10.100.20.223）向麒麟（qwer-pc / 10.100.20.22）投递「双机验收 dual-110527」，麒麟聊天区可见。反向「麒麟回传 110548」出现在 Windows 聊天区

### TC-02 文件落盘、中文名、不逃出目录、同名不覆盖
- 对应验收：A4 的落盘部分
- 前置：临时工作目录
- 步骤：上传 `说明.txt`；再以 `../../outside.txt` 上传；再传一次同名
- 期望：文件在下载目录内，中文名保留，穿越被收成基本名，同名不覆盖
- 类型：集成
- 自动化：`tests\api-smoke.ps1`；另有 `--self-check` 覆盖文件名裁剪和网卡过滤
- 结果：通过

### TC-03 设置能被读到
- 对应验收：A5
- 前置：已有 `%AppData%\lan-drop\settings.json`，设备名 omen
- 步骤：启动后看 `GET /api/info`
- 期望：返回的名称是配置里的设备名
- 类型：集成
- 结果：通过（返回 `omen`）。改设置后再读回、以及界面保存，本轮没做

### TC-04 自检：路径、虚拟网卡、广播地址
- 对应验收：A1 的协议辅助，不是双机发现
- 步骤：`landrop.exe --self-check`；ARM 包在交叉编译脚本里用 qemu 跑同一自检
- 期望：退出码 0
- 类型：单元
- 结果：Windows 通过。ARM 包在打包时自检通过。注意：无控制台的 exe 不能用 PowerShell 的 `$LASTEXITCODE` 判断，必须看进程退出码

### TC-05 手动加节点与探测
- 对应验收：A2
- 类型：手测
- 结果：未测。只确认了探测会用到的 `GET /api/info` 能返回。界面上的「添加节点」「探测」没点

### TC-06 同网段自动发现
- 对应验收：A1
- 前置：两台机器，放行 TCP 8848 与 UDP 8850
- 步骤：两边运行，等约 60 秒
- 期望：设备列表出现对端
- 类型：手测
- 结果：通过。2026-09-21 两边列表都是 1 台在线：Windows 看到 qwer-pc 10.100.20.22:8848，麒麟看到 omen 10.100.20.223:8848。文字与小文件双向都落到了对端聊天区和下载目录（Windows `downloads/from-kylin-now.txt`，麒麟 `~/landrop/downloads/from-windows.txt`）

### TC-07 大文件
- 对应验收：A4
- 步骤：本机向自己的 `POST /api/upload` 送 100MB
- 期望：落下的文件长度为 100MB；进程工作集不按文件大小上涨
- 类型：集成（只覆盖接收落盘，不覆盖窗口里点「发送文件」，也不是两台机器）
- 结果：通过。文件 100MB，工作集大约增加 0.4MB

### TC-08 网页共享列表与下载
- 对应验收：A9
- 前置：用环境变量 `LANDROP_SHARE_DIR` 指向含 `hello-share.txt` 的目录启动
- 步骤：`GET /share/`；再下载 `/share/hello-share.txt`
- 期望：列表含该文件名；下载内容正确
- 类型：集成
- 自动化：`tests\api-smoke.ps1`
- 结果：通过（`tests\api-smoke.ps1`，2026-09-21）

### TC-09 设备类型映射
- 对应验收：U2
- 步骤：`landrop.exe --self-check`
- 期望：windows→笔记本、android→手机、iPadOS→平板
- 类型：单元
- 结果：随 self-check

### TC-10 顶栏广播 Logo 左右弧
- 对应验收：U1
- 类型：手测
- 步骤：启动后看左上角蓝底图标，弧应在圆点左右
- 结果：待目视确认

### TC-11 输入区 Enter / Shift+Enter
- 对应验收：U5
- 类型：手测
- 步骤：看输入区右上角提示；再在输入框按 Enter 应发送，Shift+Enter 换行
- 期望：`Enter`、`Shift+Enter` 是浅底深灰边框的键帽，不是普通灰字
- 结果：键帽改为独立标签后待目视确认

### TC-12 发送文件夹与抖动
- 对应验收：U6、U7
- 类型：手测
- 步骤：点「发送文件夹」选含若干文件的目录；点「抖动窗口」看本机与对端是否轻抖
- 结果：待手测

### TC-13 会话顶栏与直连横幅
- 对应验收：C1、C2、C3（`docs/requirements/20260921-chat-session-header.md`）
- 前置：启动 Windows 包，左侧「加 IP」添加任意可达或不可达地址（如 `127.0.0.1:8848`）并选中
- 步骤：观察右侧消息区上方
- 期望：出现对端头像、名称、地址胶囊、「即时聊天」「文件传输 (0)」按钮，以及绿色「已建立局域网直连：…」横幅
- 类型：手测
- 结果：待目视确认

### TC-14 即时聊天 / 文件传输页壳切换
- 对应验收：C4
- 前置：已选中对端
- 步骤：点「文件传输 (0)」再点「即时聊天」
- 期望：文件页显示占位说明；聊天页回到消息与输入区；当前按钮高亮切换
- 类型：手测
- 结果：待目视确认

### TC-15 切换对端时顶栏同步
- 对应验收：C5
- 前置：列表中至少两台对端（发现或手动添加）
- 步骤：依次点选不同对端
- 期望：顶栏名称、地址、头像与横幅文案随之变化
- 类型：手测
- 结果：待目视确认

### TC-16 加 IP 对话框布局
- 对应验收：D1、D2（`docs/requirements/20260921-add-peer-dialog.md`）
- 步骤：点左侧「+ 加 IP」
- 期望：标题为「跨网段直连 / 手动添加节点」，有副标题、蓝地球图标、IP|端口、别名、系统类型|部门标签、测试连通链接、取消与「+ 添加并连接」
- 类型：手测
- 结果：待目视确认

### TC-17 对话框探测与添加
- 对应验收：D3、D4、D5、D6
- 步骤：端口保持 8848；点测试连通填 `127.0.0.1`；再选系统类型添加；部门标签随便填
- 期望：探测有连通或失败文案；添加后列表出现节点且头像随所选系统类型；部门标签不要求入库
- 类型：手测
- 结果：待手测

### TC-18 加 IP 对话框圆角无黑三角
- 对应验收：D7
- 步骤：点「+ 加 IP」，看弹窗外圈四角
- 期望：圆角外透出主窗口内容，无黑色直角三角；关闭为清晰叉号图标
- 类型：手测
- 结果：待目视确认

### TC-19 系统类型下拉样式
- 对应验收：D8
- 步骤：打开加 IP 对话框，看「系统类型」并点开列表
- 期望：外观与别名输入框同系（浅底圆角细边 + 细线箭头）；弹出列表无系统原生凹槽条
- 类型：手测
- 结果：待目视确认

### TC-20 网页共享按钮地球图标
- 对应验收：U8（`docs/requirements/20260321-ui-icons.md`）
- 步骤：启动后看顶栏「网页共享 (HTTP)」
- 期望：按钮左侧为蓝色地球线性图标，不是空白或其它符号
- 类型：手测
- 结果：待目视确认

### TC-21 设置对话框布局
- 对应验收：S1、S2、S3、S6（`docs/requirements/20260921-settings-dialog.md`）
- 步骤：点顶栏设置齿轮
- 期望：标题「局域快传设置」、副标题、深色齿轮徽标；名称/端口|线程数/下载目录及说明；两项右对齐开关；仅「保存并关闭」；四角无黑三角
- 类型：手测
- 结果：待目视确认

### TC-22 设置保存与 Nudge 开关
- 对应验收：S4、S5
- 步骤：改本机名称并保存；再关闭 Nudge 后点「抖动窗口」
- 期望：顶栏本机名称更新；关闭 Nudge 后不发送抖动
- 类型：手测
- 结果：待手测

### 缺口
- 扫码：A9 只提供链接复制，未做二维码
- 聊天气泡、文件卡片：见 `docs/requirements/20260321-ui-icons.md` 非本轮
- Ping / 链路类型 / 文件传输列表：见 `20260921-chat-session-header.md` 非本轮（C6）
- 部门标签入库：见 `20260921-add-peer-dialog.md` 非本轮（D6）
- 并发线程池与通知声播放：见 `20260921-settings-dialog.md` 非本轮
- 仓库里的 Go 测试（`go test ./...`）测的是旧 Go 进程，不能代替 Qt 客户端
