# MVP 验收用例

对应需求：`docs/requirements/20260320-局域快传-mvp.md`  
当前交付物是 Qt 客户端，不是 Go 里那套网页接口。下面标的是 2026-09-21 在本机对 Qt 包的执行结果。

### TC-01 文本收发与中文
- 对应验收：A3
- 前置：Windows 包 `dist/windows-x64/landrop.exe` 已启动
- 步骤：向 `POST /api/inbox` 投递 UTF-8 中文
- 期望：接口返回成功，对端聊天区能看到 UTF-8 中文
- 类型：集成
- 自动化：`powershell -File tests\api-smoke.ps1`
- 结果：通过。2026-09-21 Windows（omen / 10.100.20.223）向麒麟（qwer-pc / 10.100.20.22）投递「双机验收 dual-110527」，麒麟聊天区可见。反向「麒麟回传 110548」出现在 Windows 聊天区

### TC-02 文件落盘、中文名、不逃出目录、同名不覆盖
- 对应验收：A4 的落盘部分
- 前置：临时工作目录
- 步骤：上传 `说明.txt`；再以 `../../outside.txt` 上传；再传一次同名
- 期望：文件在下载目录内，中文名保留，穿越被收成基本名，同名不覆盖
- 类型：集成
- 自动化：`tests\api-smoke.ps1`；另有 `--self-check` 覆盖文件名裁剪和网卡过滤
- 结果：通过

### TC-03 设置能被读到
- 对应验收：A5
- 前置：已有 `%AppData%\lan-drop\settings.json`，设备名 omen
- 步骤：启动后看 `GET /api/info`
- 期望：返回的名称是配置里的设备名
- 类型：集成
- 结果：通过（返回 `omen`）。改设置后再读回、以及界面保存，本轮没做

### TC-04 自检：路径、虚拟网卡、广播地址
- 对应验收：A1 的协议辅助，不是双机发现
- 步骤：`landrop.exe --self-check`；ARM 包在交叉编译脚本里用 qemu 跑同一自检
- 期望：退出码 0
- 类型：单元
- 结果：Windows 通过。ARM 包在打包时自检通过。注意：无控制台的 exe 不能用 PowerShell 的 `$LASTEXITCODE` 判断，必须看进程退出码

### TC-05 手动加节点与探测
- 对应验收：A2；P1–P3（`docs/requirements/20260922-manual-peers-persist.md`）
- 步骤：点「+ 加 IP」添加可达节点 → 看列表 → 完全退出再启动
- 期望：列表出现该节点；`settings.json` 有 `manualPeers`；重启后节点仍在（别名/系统类型保留）
- 类型：手测 / 集成
- 结果：自检覆盖读写往返；界面重启待手测

### TC-37 删除手动节点
- 对应验收：R1–R4（`docs/requirements/20260922-remove-manual-peer.md`）
- 步骤：添加手动节点 → 右键「删除手动节点」确认 → 再对自动发现节点右键/按 Delete → 重启
- 期望：手动节点消失且 settings 无该项、重启不恢复；自动发现节点不可删
- 类型：手测 / 集成
- 结果：自检覆盖 removeManual；界面待手测

### TC-06 同网段自动发现
- 对应验收：A1
- 前置：两台机器，放行 TCP 8848 与 UDP 8850
- 步骤：两边运行，等约 60 秒
- 期望：设备列表出现对端
- 类型：手测
- 结果：通过。2026-09-21 两边列表都是 1 台在线：Windows 看到 qwer-pc 10.100.20.22:8848，麒麟看到 omen 10.100.20.223:8848。文字与小文件双向都落到了对端聊天区和下载目录（Windows `downloads/from-kylin-now.txt`，麒麟 `~/landrop/downloads/from-windows.txt`）

### TC-07 大文件
- 对应验收：A4
- 步骤：本机向自己的 `POST /api/upload` 送 100MB
- 期望：落下的文件长度为 100MB；进程工作集不按文件大小上涨
- 类型：集成（只覆盖接收落盘，不覆盖窗口里点「发送文件」，也不是两台机器）
- 结果：通过。文件 100MB，工作集大约增加 0.4MB

### TC-08 网页共享列表与下载
- 对应验收：A9
- 前置：用环境变量 `LANDROP_SHARE_DIR` 指向含 `hello-share.txt` 的目录启动
- 步骤：`GET /share/`；再下载 `/share/hello-share.txt`
- 期望：列表含该文件名；下载内容正确
- 类型：集成
- 自动化：`tests\api-smoke.ps1`
- 结果：通过（`tests\api-smoke.ps1`，2026-09-21）

### TC-09 设备类型映射
- 对应验收：U2；O1–O3（`docs/requirements/20260922-arm-os-not-rpi.md`）；A1–A3（`docs/requirements/20260922-avatar-name-initial.md`）
- 步骤：`landrop.exe --self-check`；看列表与聊天头像
- 期望：windows→笔记本角标、android→手机、iPadOS→平板；头像为机器名首字（如 qwer-pc→Q），非系统 SVG「树」；发出气泡头像为本机设备名首字
- 类型：单元 + 手测
- 结果：随 self-check；头像待手测

### TC-10 顶栏广播 Logo 左右弧
- 对应验收：U1
- 类型：手测
- 步骤：启动后看左上角蓝底图标，弧应在圆点左右
- 结果：待目视确认

### TC-11 输入区 Enter / Shift+Enter
- 对应验收：U5
- 类型：手测
- 步骤：看输入区右上角提示；再在输入框按 Enter 应发送，Shift+Enter 换行
- 期望：`Enter`、`Shift+Enter` 是浅底深灰边框的键帽，不是普通灰字
- 结果：键帽改为独立标签后待目视确认

### TC-12 发送文件夹与抖动
- 对应验收：U6、U7；文件夹说明见 TC-62
- 类型：手测
- 步骤：点「发送文件夹」选含若干文件的目录；点「抖动窗口」看本机与对端是否轻抖
- 结果：待手测

### TC-62 发文件夹仅顶层说明
- 对应验收：D1–D4（`docs/requirements/20260922-folder-top-only-hint.md`）
- 步骤：① 仅顶层文件的目录 → 看系统条；② 顶层有文件+子目录 → 取消一次再确认；③ 仅子目录无顶层文件；④ 拖文件夹进聊天
- 期望：系统条含「顶层/不含子目录」；有子目录时先确认；仅子目录有说明不发；拖放多文件也标明仅顶层
- 类型：手测
- 结果：待手测

### TC-13 会话顶栏与直连横幅
- 对应验收：C1、C2、C3（`docs/requirements/20260921-chat-session-header.md`）
- 前置：启动 Windows 包，左侧「加 IP」添加任意可达或不可达地址（如 `127.0.0.1:8848`）并选中
- 步骤：观察右侧消息区上方
- 期望：出现对端头像、名称、地址胶囊、「即时聊天」「文件传输 (0)」按钮，以及绿色「已建立局域网直连：…」横幅
- 类型：手测
- 结果：待目视确认

### TC-14 即时聊天 / 文件传输页壳切换
- 对应验收：C4
- 前置：已选中对端
- 步骤：点「文件传输 (0)」再点「即时聊天」
- 期望：文件页显示占位说明；聊天页回到消息与输入区；当前按钮高亮切换
- 类型：手测
- 结果：待目视确认

### TC-15 切换对端时顶栏同步
- 对应验收：C5
- 前置：列表中至少两台对端（发现或手动添加）
- 步骤：依次点选不同对端
- 期望：顶栏名称、地址、头像与横幅文案随之变化
- 类型：手测
- 结果：待目视确认

### TC-16 加 IP 对话框布局
- 对应验收：D1、D2（`docs/requirements/20260921-add-peer-dialog.md`）
- 步骤：点左侧「+ 加 IP」
- 期望：标题为「跨网段直连 / 手动添加节点」，有副标题、蓝地球图标、IP|端口、别名、系统类型|部门标签、测试连通链接、取消与「+ 添加并连接」
- 类型：手测
- 结果：待目视确认

### TC-17 对话框探测与添加
- 对应验收：D3、D4、D5（`docs/requirements/20260921-add-peer-dialog.md`）
- 步骤：端口保持 8848；点测试连通填 `127.0.0.1`；再选系统类型添加；部门标签随便填
- 期望：探测有连通或失败文案；添加后列表出现节点且头像随所选系统类型
- 类型：手测
- 结果：待手测

### TC-58 手动节点部门标签入库
- 对应验收：T1–T5（`docs/requirements/20260922-peer-tag.md`）
- 步骤：加 IP 填标签「工控」→ 看 settings.json → 退出再开 → 搜索「工控」；另测不填标签；`--self-check`
- 期望：tag 落盘且重启可见；列表/顶栏有标签；搜索可筛；无 tag 旧配置不崩；自检通过
- 类型：手测 / 集成
- 结果：`--self-check` 含 tag；界面待手测

### TC-60 右键编辑手动节点
- 对应验收：E1–E5（`docs/requirements/20260922-edit-manual-peer.md`）
- 步骤：手动节点右键「编辑别名 / 标签…」改别名与标签 → 看列表/顶栏 → 退出再开；另对自动发现节点确认无编辑或禁用；`--self-check`
- 期望：预填当前值；保存后展示与 settings 同步；重启仍在；自动发现不可编；自检覆盖同址覆盖 tag
- 类型：手测 / 集成
- 结果：`--self-check` 含覆盖；界面待手测

### TC-18 加 IP 对话框圆角无黑三角
- 对应验收：D7
- 步骤：点「+ 加 IP」，看弹窗外圈四角
- 期望：圆角外透出主窗口内容，无黑色直角三角；关闭为清晰叉号图标
- 类型：手测
- 结果：待目视确认

### TC-19 系统类型下拉样式
- 对应验收：D8
- 步骤：打开加 IP 对话框，看「系统类型」并点开列表
- 期望：外观与别名输入框同系（浅底圆角细边 + 细线箭头）；弹出列表无系统原生凹槽条
- 类型：手测
- 结果：待目视确认

### TC-20 网页共享按钮地球图标
- 对应验收：U8（`docs/requirements/20260321-ui-icons.md`）
- 步骤：启动后看顶栏「网页共享 (HTTP)」
- 期望：按钮左侧为蓝色地球线性图标，不是空白或其它符号
- 类型：手测
- 结果：待目视确认

### TC-21 设置对话框布局
- 对应验收：S1、S2、S3、S6、S8（`docs/requirements/20260921-settings-dialog.md`）；B1（`docs/requirements/20260922-settings-browse-dir.md`）；H1（`docs/requirements/20260922-threads-honest.md`）；A1（`docs/requirements/20260922-copy-honest-search.md`）
- 步骤：点顶栏设置齿轮
- 期望：标题「局域快传设置」、副标题含「通知偏好」（非仅「接收提示」）、深色齿轮徽标；名称/端口|线程数（灰显+暂不生效说明）/下载目录+「浏览…」及说明；两项右对齐开关；仅「保存并关闭」；四角无黑三角
- 类型：手测
- 结果：待目视确认

### TC-65 搜索占位与无匹配空态
- 对应验收：A2–A4（`docs/requirements/20260922-copy-honest-search.md`）
- 步骤：看搜索占位；有对端时输入不可能匹配的关键字；再清空搜索
- 期望：占位含标签；无匹配时右侧有说明；清空后列表恢复
- 类型：手测
- 结果：待手测

### TC-66 上传中非打断提示
- 对应验收：B1–B4（`docs/requirements/20260922-busy-upload-soft.md`）
- 步骤：发大文件过程中再点发送文件/拖入文件/点旧失败重试；看是否弹框；传完后再发一次
- 期望：无模态框；当前传输继续；成功路径以入队系统条为主（见 TC-67）；传完可再发
- 类型：手测
- 结果：待手测

### TC-67 发送中自动入队
- 对应验收：Q1–Q5（`docs/requirements/20260922-upload-queue-append.md`）
- 步骤：发大文件过程中再拖入 2 个不同文件；再点发送选一个；再拖同一路径一次；另测一次发送中点失败重试；再测中途人为断网使当前失败
- 期望：系统条「已加入发送队列」；传完自动续传；重复路径不重复排队；重试无模态且入队；失败后剩余队列清空不再续传
- 类型：手测
- 结果：待手测

### TC-68 取消当前发送
- 对应验收：C1–C5（`docs/requirements/20260922-cancel-upload.md`）
- 步骤：发大文件并再入队 1～2 个；看进度旁「取消」；点取消；切到文件页再发一次看取消；取消后再发一个小文件
- 期望：发送中可见取消、空闲隐藏；点后停止且排队不续；系统条「已取消发送」无重试；文件页同样可取消；取消后可再发成功
- 类型：手测
- 结果：待手测

### TC-64 并发线程数诚实化
- 对应验收：H1–H3（`docs/requirements/20260922-threads-honest.md`）
- 步骤：打开设置看线程数不可改与说明；改设备名保存；再看 settings.json 的 transferThreads 未因本操作乱变
- 期望：框禁用且有单队列说明；其它项可保存；上传仍单文件排队
- 类型：手测
- 结果：待手测

### TC-38 设置下载目录浏览
- 对应验收：B1–B3（`docs/requirements/20260922-settings-browse-dir.md`）
- 步骤：打开设置 → 点「浏览…」选一文件夹 → 确认 → 再点浏览后取消 → 保存并关闭
- 期望：确认后路径写入输入框；取消不改；保存后新目录生效
- 类型：手测
- 结果：待手测

### TC-39 系统托盘常驻
- 对应验收：T1–T3（`docs/requirements/20260922-system-tray.md`）
- 步骤：点标题栏 ✕ → 看托盘 → 单击托盘或「显示主窗口」→ 再 ✕ → 托盘右键「退出局域快传」
- 期望：✕ 后窗口隐藏进程仍在、可收文件；可恢复窗口；退出后进程结束
- 类型：手测
- 结果：待手测

### TC-40 聊天区拖放发送
- 对应验收：D1–D4（`docs/requirements/20260922-chat-drop-send.md`）
- 步骤：选中对端 → 拖一个文件进聊天区；再拖含多文件的文件夹；未选对端时拖一下
- 期望：单文件发出；文件夹顶层文件依次发；未选对端有提示；发送按钮行为不变
- 类型：手测
- 结果：待手测

### TC-40b 聊天区 Ctrl+V 粘贴发送
- 对应验收：P1–P4（`docs/requirements/20260922-chat-paste-send.md`）
- 步骤：选中对端 → 资源管理器复制文件 → 输入框 Ctrl+V；再粘贴普通文字；未选对端时复制文件再 Ctrl+V
- 期望：文件发出；纯文字仍粘贴进输入框；未选对端有提示；拖放仍可用
- 类型：手测
- 结果：待手测

### TC-41 打开下载目录
- 对应验收：O1–O4（`docs/requirements/20260922-open-download-dir.md`）
- 步骤：点顶栏文件夹图标；再托盘右键「打开下载目录」
- 期望：均打开当前设置中的下载目录；目录不存在时会先创建
- 类型：手测
- 结果：待手测

### TC-42 后台收件托盘气泡
- 对应验收：N1–N5（`docs/requirements/20260922-tray-notify.md`）；J1–J4（`docs/requirements/20260922-tray-jump-peer.md`）
- 步骤：点 ✕ 进托盘 → 首次提示可点（不切会话）→ 对端发文字与文件 → 点收件提示；再仅点托盘图标恢复
- 期望：收件提示含发送方/文件名；点提示恢复并选中该对端、聊天滚到最新；仅点图标不改选中；前台不弹
- 类型：手测
- 结果：待手测

### TC-44 对端未读角标
- 对应验收：U1–U4（`docs/requirements/20260922-unread-badge.md`）
- 步骤：选中 A → B 发文字；点 B；再隐藏窗口让当前对端发文件后点托盘图标恢复；另测抖动不计未读
- 期望：B 名旁出现 `· N`；点进后消失；托盘恢复清当前未读；抖动不加未读
- 类型：手测
- 结果：待手测

### TC-43 ARM 麒麟中文输入
- 对应验收：A1–A3（`docs/requirements/20260922-kylin-ime-chinese.md`）
- 步骤：麒麟上用 `landrop.sh` 启动 → 聊天框切中文输入法打字 → 设置改中文设备名
- 期望：可出候选并上屏；进程有 `QT_IM_MODULE=fcitx`；`plugins/platforminputcontexts/` 能解析到 fcitx 插件
- 类型：手测（22 机）
- 结果：待手测

### TC-45 发送侧气泡内进度
- 对应验收：P1–P5（`docs/requirements/20260922-bubble-send-progress.md`）
- 步骤：选中对端发一个较大文件；观察聊天与「文件传输」Tab；传完后点打开；另测断网或关对端触发失败；传输中切到其他对端再切回
- 期望：立刻出现「发送中 N%」且条未满；完成后同卡满条+「传输完成」+可打开；失败无假完成卡；切回仍见正确进度
- 类型：手测
- 结果：待手测

### TC-46 文件发送失败重试
- 对应验收：R1–R4（`docs/requirements/20260922-send-fail-retry.md`）
- 步骤：关对端或断网后发文件 → 看失败条「重试」→ 恢复对端后点重试；另在上传中点旧失败的重试；再删源文件后点重试
- 期望：失败条有「重试」；恢复后重试可传成；上传中点重试有进度区「请稍候」类提示且不弹模态、不打断；源文件没了有失败提示不崩
- 类型：手测
- 结果：待手测

### TC-47 聊天拖放虚线反馈
- 对应验收：V1–V4（`docs/requirements/20260922-chat-drop-visual.md`）
- 步骤：选中对端 → 拖文件进入会话区（聊天/输入框之间移动）→ 拖出 → 再拖入松手发送
- 期望：进入出现蓝虚线与「松手发送到 …」；区内移动不闪灭；拖出或松手后消失；文件仍能发出
- 类型：手测
- 结果：待手测

### TC-48 聊天贴底滚动
- 对应验收：S1–S4（`docs/requirements/20260922-chat-stick-bottom.md`）
- 步骤：选中对端发较大文件 → 上传中上翻看历史；再滚回底部附近继续传；再切换对端后切回
- 期望：上翻时进度仍更新但视口不被拽回；贴底时自动跟最新；切回会话落在最新附近；短会话无滚动条不崩
- 类型：手测
- 结果：待手测

### TC-49 窗口位置与大小记忆
- 对应验收：G1–G4（`docs/requirements/20260922-window-geometry.md`）
- 步骤：拉大并移动窗口 → ✕ 进托盘再打开；再最大化后进托盘再打开；`--self-check`
- 期望：普通尺寸/位置恢复；最大化可恢复；自检含 window geom；缺字段启动不崩
- 类型：手测 / 集成
- 结果：`--self-check` 含 window geom；界面待手测

### TC-50 有新消息回到底部
- 对应验收：J1–J4（`docs/requirements/20260922-jump-bottom.md`）
- 步骤：选中对端上翻 → 对端发文字；再上翻后本机发文件至完成；上翻只看发送进度；手动滚回底部；切换对端
- 期望：文字/文件完成出现「有新消息 ↓」可点回底部；进度 tick 不刷按钮；贴底或切会话后按钮消失
- 类型：手测
- 结果：待手测

### TC-51 一键复制本机地址
- 对应验收：C1–C4（`docs/requirements/20260922-copy-local-addr.md`）
- 步骤：点击顶栏本机胶囊 → 粘贴到记事本；改传输端口保存后再点复制；拖窗口时勿点在胶囊上误测
- 期望：剪贴板为 `IP:端口`；有「已复制」提示；点击胶囊不拖窗；改端口后复制含新端口
- 类型：手测
- 结果：待手测

### TC-52 左侧栏宽度记忆
- 对应验收：W1–W4（`docs/requirements/20260922-side-width.md`）
- 步骤：拖分割条拉宽侧栏 → ✕ 进托盘再打开；再试拖到极限；`--self-check`
- 期望：宽度恢复；约 220–480 限制；缺字段默认约 300；自检含 sideWidth
- 类型：手测 / 集成
- 结果：`--self-check` 含 sideWidth；界面待手测

### TC-53 记住上次选中的对端
- 对应验收：L1–L4（`docs/requirements/20260922-last-peer.md`）
- 步骤：选中对端 B → 退出再开；删掉/离线使 B 不在列表后再开；运行中改选 A 后等列表刷新；`--self-check`
- 期望：B 仍在则自动选中；不在则回退不崩；刷新不把 A 打回旧 lastPeer；自检含 lastPeer
- 类型：手测 / 集成
- 结果：`--self-check` 含 lastPeer；界面待手测

### TC-54 会话顶栏复制对端地址
- 对应验收：P1–P4（`docs/requirements/20260922-copy-peer-addr.md`）
- 步骤：选中对端 A → 点顶栏地址粘贴核对；再选对端 B 再点；再点本机胶囊确认互不干扰
- 期望：剪贴板分别为 A、B 的 `IP:端口`；有「已复制」提示；本机复制仍正常
- 类型：手测
- 结果：待手测

### TC-55 聊天记录落盘
- 对应验收：H1–H5（`docs/requirements/20260922-chat-persist.md`）
- 步骤：与对端互发文字与文件 → ✕ 进托盘或退出再开；另测无 chat.json / 损坏文件启动；`--self-check`
- 期望：历史气泡与完成文件卡仍在；损坏不崩；自检含 chat 往返且跳过进行中文件卡
- 类型：手测 / 集成
- 结果：`--self-check` 含 chat 往返；界面待手测

### TC-56 清空当前会话聊天记录
- 对应验收：X1–X5（`docs/requirements/20260922-clear-chat.md`）
- 步骤：选对端有若干气泡 → 右键「清空聊天记录」→ 取消一次仍在；再确认清空；退出再开；另测发送中清空应提示；确认下载目录文件仍在、手动节点仍在
- 期望：确认后聊天/文件 Tab 空；重启仍空；取消无变；发送中不清；节点与落盘文件保留
- 类型：手测
- 结果：待手测

### TC-57 接收方气泡进度
- 对应验收：R1–R5（`docs/requirements/20260922-recv-progress.md`）
- 步骤：对端发较大文件 → 本机聊天出现「接收中 N%」→ 完成后可打开；退出再开确认进行中未残留、完成卡仍在；`--self-check`
- 期望：完成前有进度；完成后可开；chat.json 不存进行中 InFile；自检跳过接收中卡
- 类型：手测 / 集成
- 结果：`--self-check` 含跳过接收中；界面待手测

### TC-61 接收失败可见提示
- 对应验收：F1–F4（`docs/requirements/20260922-recv-fail-notice.md`）
- 步骤：对端开始发大文件 → 本机出现接收中 → 中断对端发送；看失败条；关通知声再测；退出再开
- 期望：进度卡消失、红色失败条含文件名、无重试链；开时有声关时无；失败条落盘仍在
- 类型：手测
- 结果：待手测

### TC-22 设置保存与 Nudge 开关
- 对应验收：S4、S5
- 步骤：改本机名称并保存；再关闭 Nudge 后点「抖动窗口」
- 期望：顶栏本机名称更新；关闭 Nudge 后不发送抖动
- 类型：手测
- 结果：待手测

### TC-23 设置开关勾选对号
- 对应验收：S7
- 步骤：打开设置，看两项开关勾选态；再取消勾选
- 期望：勾选时蓝框内有白色对号；取消后为空框
- 类型：手测
- 结果：待目视确认

### TC-24 网页共享对话框布局
- 对应验收：W1、W2、W3、W6（`docs/requirements/20260921-web-share-dialog.md`）
- 步骤：点顶栏「网页共享」
- 期望：标题「本机 HTTP 网页共享服务」、副标题、蓝色分享徽标、运行/暂停状态；左卡地址含 `/share/` 与「复制网址」「本机浏览器自测」；右卡二维码与左卡等高；四角无黑三角
- 类型：手测
- 结果：待目视确认

### TC-25 网页共享列表与暂停
- 对应验收：W4、W5
- 步骤：点「添加文件到共享」选一个含顶层文件的目录；再点「暂停 HTTP 服务」；再点「开启 HTTP 服务」
- 期望：列表出现文件名与大小且数量一致；暂停后列表为空、按钮变为开启；再次开启后列表恢复
- 类型：手测
- 结果：待手测

### TC-26 网页共享二维码
- 对应验收：W3
- 步骤：运行 `landrop --self-check`（或既有自检入口）
- 期望：自检通过；二维码对 `http://192.168.1.108:8848/share/` 生成，且左上、右上定位角为深色模块
- 类型：集成
- 结果：通过（`--self-check` 退出码 0）

### TC-27 网页共享对话框图标
- 对应验收：W7（`docs/requirements/20260921-web-share-dialog.md`）
- 步骤：打开网页共享对话框，看左上徽标与底栏「暂停/开启 HTTP 服务」按钮
- 期望：徽标为蓝底圆角 + 白色三节点分享连线；按钮左侧为深灰电源开关符号
- 类型：手测
- 结果：待目视确认

### TC-28 直连胶囊
- 对应验收：M1（`docs/requirements/20260921-chat-message-bubbles.md`）；C1–C4（`docs/requirements/20260922-conn-banner-capsule.md`）
- 步骤：选中一台在线对端，看会话顶栏下方状态条
- 期望：浅灰底上居中白底真胶囊（细灰边、两端半圆）；绿盾；「已建立局域网直连：」灰字 + 设备名加粗深色 + `(IP:端口)` 灰字
- 类型：手测
- 结果：待目视确认

### TC-29 聊天气泡与送达
- 对应验收：M2、M3、M7；B1–B3（`docs/requirements/20260922-chat-avatar-bubble.md`）
- 步骤：向对端发一条文字；对端回一条；看头像与气泡外形；切换另一台再切回来
- 期望：头像约 40px 圆角方；发出为右蓝圆角矩形气泡「我 + 时间 + ✓✓ 已送达 - Nms」；收到为左白圆角矩形气泡；切回后历史仍正确
- 类型：手测
- 结果：待手测

### TC-30 文件卡与代码块
- 对应验收：M4、M5、M6；F1–F4（`docs/requirements/20260922-file-card-open.md`）
- 步骤：收一个文件 → 点「打开文件」与「打开所在目录」；再发一段以 ```bash 开头的代码围栏；点抖动
- 期望：打开文件用默认程序打开该文件；打开所在目录进父目录；代码块深色且「复制」可用；抖动为居中提示
- 类型：手测
- 结果：待手测

### TC-31 会话 Ping 与链路
- 对应验收：P1、P2、P4（`docs/requirements/20260921-ping-and-files.md`）
- 步骤：选中在线对端，看顶栏副行；再换一台对端
- 期望：几秒内出现 `Ping` 毫秒或「超时」，以及本机网卡速率（如千兆/2.5 GbE 网线）；换对端后时延跟着变，不沿用上一台
- 类型：手测
- 结果：待手测

### TC-32 文件传输列表
- 对应验收：P3
- 步骤：向当前对端发送一个文件，再打开「文件传输」
- 期望：按钮数字大于 0，页内能看到该文件名与大小；进行中时页顶有进度文字
- 类型：手测
- 结果：待手测

### TC-33 通知声开关
- 对应验收：N1–N4（`docs/requirements/20260922-sound-notification.md`）
- 步骤：保持通知声开启，对端发文字与文件；再关掉通知声保存后重发；再发一次抖动
- 期望：开时收文字/文件各有一声系统提示；关后无声；抖动不额外 beep
- 类型：手测
- 结果：待手测

### TC-59 发送文件完成通知声
- 对应验收：S1–S4（`docs/requirements/20260922-send-complete-sound.md`）
- 步骤：通知声开 → 本机发文件至完成听一声；关开关保存后再发完成；另发一条文字确认无额外声
- 期望：开时发文件完成有一声；关后无声；发文字成功不因本功能多播；收件行为不变
- 类型：手测
- 结果：待手测

### TC-63 自定义通知铃声
- 对应验收：C1–C5（`docs/requirements/20260922-custom-ringtone.md`）
- 步骤：设置选 wav → 试听 → 保存 → 收/发文件听铃声；恢复默认再测；关通知声；无效路径；`--self-check`
- 期望：有效 wav 生效；默认回系统音；关开关收发静音但试听可用；无效回落不崩；自检含 soundFile
- 类型：手测 / 集成
- 结果：`--self-check` 含 soundFile；界面待手测

### TC-34 会话顶栏主机名
- 对应验收：H1–H3（`docs/requirements/20260922-peer-hostname.md`）
- 步骤：本机 `landrop --self-check`；选中对端看副行首段；可用浏览器打开 `http://本机IP:端口/api/info` 看 `hostname`
- 期望：自检含 hostname；`/api/info` 有 hostname 字段；副行为「主机名 · Ping · 链路」，无字段时回退设备名且不崩
- 类型：手测 / 集成
- 结果：自检通过（`--self-check` 含 hostname）；界面待双机目视

### TC-35 网页共享添加文件与拖放
- 对应验收：F1–F5、W8（`docs/requirements/20260922-share-add-files.md`）
- 步骤：打开网页共享 →「添加文件」多选；再拖一个文件到虚线区；同名再加一次；点行内删除；浏览器打开 `/share/`
- 期望：列表与数量同步；同名出现后缀文件；删除后列表与下载页无该文件；源文件仍在；`--self-check` 含复制去重
- 类型：手测 / 集成
- 结果：自检通过；界面待手测

### TC-36 顶栏品牌图标与文字高度对齐
- 对应验收：U9（`docs/requirements/20260321-ui-icons.md`）
- 步骤：启动后看左上蓝图标与「局域快传 / 在线 · …」两行
- 期望：文字块高度与图标同为约 36px，整体垂直居中；标题顶边与图标顶边齐，状态行底边与图标底边齐
- 类型：手测
- 结果：待目视确认

### 缺口
- 扫码：对话框已出二维码；旧条目 A9 仅复制链接的说法以 W3 为准
- 网页共享添加文件/拖放/行内删除：见 `20260922-share-add-files.md`（本轮已落地）
- 聊天气泡、文件卡片、Ping/链路/传输 Tab、hostname：已落地，见对应需求文档
- 手动节点持久化：见 `20260922-manual-peers-persist.md`（本轮已落地）
- 手动节点删除：见 `20260922-remove-manual-peer.md`（本轮已落地）
- 部门标签入库：见 `20260922-peer-tag.md`（本轮已落地）
- 右键编辑手动节点别名/标签：见 `20260922-edit-manual-peer.md`（本轮已落地）
- 自定义铃声：见 `20260922-custom-ringtone.md`（本轮已落地）
- 并发线程数诚实化：见 `20260922-threads-honest.md`（本轮已落地）
- 通知声（收件）：见 `20260922-sound-notification.md`
- 发送完成通知声：见 `20260922-send-complete-sound.md`（本轮已落地）
- 真并发上传线程池：仍非本轮（大项）
- 设置下载目录浏览按钮：见 `20260922-settings-browse-dir.md`（本轮已落地）
- 系统托盘常驻：见 `20260922-system-tray.md`（本轮已落地）
- 聊天区拖放发送：见 `20260922-chat-drop-send.md`（本轮已落地）
- 打开下载目录：见 `20260922-open-download-dir.md`（本轮已落地）
- 后台收件托盘气泡：见 `20260922-tray-notify.md`（本轮已落地）
- 气泡内发送进度：见 `20260922-bubble-send-progress.md`（本轮已落地）
- 文件发送失败重试：见 `20260922-send-fail-retry.md`（本轮已落地）
- 聊天拖放虚线反馈：见 `20260922-chat-drop-visual.md`（本轮已落地）
- 聊天贴底滚动：见 `20260922-chat-stick-bottom.md`（本轮已落地）
- 窗口位置与大小记忆：见 `20260922-window-geometry.md`（本轮已落地）
- 有新消息回到底部：见 `20260922-jump-bottom.md`（本轮已落地）
- 一键复制本机地址：见 `20260922-copy-local-addr.md`（本轮已落地）
- 左侧栏宽度记忆：见 `20260922-side-width.md`（本轮已落地）
- 记住上次选中对端：见 `20260922-last-peer.md`（本轮已落地）
- 会话顶栏复制对端地址：见 `20260922-copy-peer-addr.md`（本轮已落地）
- 聊天记录落盘：见 `20260922-chat-persist.md`（本轮已落地）
- 清空当前会话聊天记录：见 `20260922-clear-chat.md`（本轮已落地）
- 接收方气泡进度：见 `20260922-recv-progress.md`（本轮已落地）
- 接收失败可见提示：见 `20260922-recv-fail-notice.md`（本轮已落地）
- 发文件夹仅顶层说明：见 `20260922-folder-top-only-hint.md`（本轮已落地）
- 设置副标题与搜索空态：见 `20260922-copy-honest-search.md`（本轮已落地）
- 上传中非打断提示：见 `20260922-busy-upload-soft.md`（本轮已落地）
- 发送中自动入队：见 `20260922-upload-queue-append.md`（本轮已落地）
- 取消当前发送：见 `20260922-cancel-upload.md`（本轮已落地）
- 仓库里的 Go 测试（`go test ./...`）测的是旧 Go 进程，不能代替 Qt 客户端
