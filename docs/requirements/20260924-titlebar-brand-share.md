# 顶栏品牌状态与网页共享瘦身

- 状态：已实现（待手测）
- 背景：左侧「自动发现中」绿底厚边像膏药抢主标题；右侧「网页共享」比同行图标又高又厚，顶栏失衡。
- 目标：状态改为绿点+字、无底框；共享钮高度约 28px、轻边轻底，与右侧工具图标同轴。
- 非目标：改发现逻辑；重做本机胶囊；改窗口控制语义。

## 用户故事

作为使用者，我希望顶栏一眼是品牌名，状态安静陪衬；网页共享是同行小胶囊，不压过文件夹/设置。

## 行为

1. 「局域快传」约 14px 粗体；下方状态行：7px 圆点 + 11px 字，容器透明无边。
2. 正常：字色森林绿 `#059669`；异常：琥珀字（保留圆点色切换）。
3. 「网页共享」固定高 28px，底 `#F0F7FF`、边 `#DBEAFE`、圆角 6、字重 500；共享中仍浅绿系但不加厚。

## 验收标准

- [x] T1：状态无绿/黄底框，不抢主标题（实现已对齐，待手测）
- [x] T2：品牌两行与左侧 Logo 视觉齐平、有呼吸（实现已对齐，待手测）
- [x] T3：网页共享与右侧图标大致同高同轴，不再显「大面包」（实现已对齐，待手测）

## 影响范围

- `src/mainwindow.cpp`
- `docs/test/mvp-cases.md`

## 待确认

- 无

## 建议下一手

手测 T1–T3。
