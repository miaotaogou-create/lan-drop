# 跳底白卡、离线条、顶栏地址与气泡间距

- 状态：已实现（待手测）
- 背景：跳底钮仍抢主色蓝；离线条外包灰带多余；顶栏地址 chip 与本机 pill 两套皮；聊天气泡纵向偏挤。
- 目标：跳底改白卡蓝字；离线只留暖色浮卡；peerAddr 对齐 hostPill；消息行距略增。
- 非目标：文件 Tab 再加独立数量胶囊；改跳底逻辑。

## 用户故事

作为使用者，我希望：有新消息提示不抢发送钮；离线提示干净；顶栏地址和本机 pill 同系；长聊天更透气。

## 行为

1. `#jumpBottomBtn`：白底、浅边、蓝字；保留浮起阴影。
2. `#connBannerHost` 背景透明，去掉横贯灰带。
3. `#peerAddr`：白底 + `#e2e8f0` 边，悬停仍浅蓝；与 hostPill 同系。
4. 聊天/文件列表条目外边距约 `14px` → `18px`。

## 验收标准

- [x] J1：有新消息 ↓ 为白卡蓝字（实现已对齐，待手测）
- [x] J2：离线时无横贯灰带，仅见暖色浮卡（实现已对齐，待手测）
- [x] J3：顶栏 IP chip 与本机 pill 同系白卡（实现已对齐，待手测）
- [x] J4：消息纵向间距肉眼更松（实现已对齐，待手测）

## 影响范围

- `src/mainwindow.cpp`
- `src/chatrender.cpp`
- `docs/test/mvp-cases.md`

## 待确认

- 无

## 建议下一手

手测 J1–J4。
