# 会话顶栏可打开下载目录

- 状态：已废弃（入口收敛）
- 背景：收完文件后常在会话里；顶栏已有下载目录入口，但视线在会话区时还要抬头找，多一步。
- 目标：~~会话顶栏增加打开下载目录按钮~~ → 已被 `20260922-folder-icon-dedupe.md` 取代：与顶栏功能完全重复且图标撞车，会话旁入口已移除；打开下载只保留顶栏 + 托盘，并用无加号文件夹图标与「发送文件夹」区分。
- 非目标：改下载路径；文件 Tab 内再列目录树。

## 用户故事

作为使用者，我在和某台设备传文件时，希望就近打开本机下载目录核对刚收到的文件。

## 行为

1. ~~会话顶栏 Tab 右侧有「下载目录」按钮~~（已移除）。
2. 打开下载目录：顶栏图标按钮 + 托盘菜单（见 `20260922-open-download-dir.md` / `20260922-folder-icon-dedupe.md`）。

## 验收标准

- [x] D1：~~选中对端后会话顶栏可见下载目录入口~~ → 改由顶栏入口承担（见 folder-icon-dedupe F1/F2）
- [x] D2：点击能打开接收目录（顶栏/托盘，实现已对齐）

## 影响范围

- `src/mainwindow.cpp`
- `docs/requirements/20260922-folder-icon-dedupe.md`
- `docs/test/mvp-cases.md`

## 待确认

- 无

## 建议下一手

按 `20260922-folder-icon-dedupe.md` 手测。
